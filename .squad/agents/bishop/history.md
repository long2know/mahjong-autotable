# Project Context

- **Owner:** Stephen Long
- **Project:** Changsha-first Mahjong game built from pwmarcz/autotable, with expanded Chinese rules planned
- **Stack:** .NET 10 backend, EF Core + SQLite initially, optional React + Fluent UI 9 + TypeScript + Vite frontend modernization, single-image Docker deployment
- **Created:** 2026-04-20

## Learnings (summarized 2026-07-27T01-56-23-811-07-00)

> Full history (256954 B, 92 entries) preserved verbatim in `history-archive.md`. Most-recent entries retained below.

📌 Team update (2026-06-03T16:30:00Z): W25 — Bot autonomy + multi-game audit. Stephen's directive: verify (a) bots are autonomous, (b) each game has its own state, (c) late joiners get current state, (d) bots claim when an opportunity arises, (e) URL `?botDifficulty=` actually changes strategy, (f) F-scenarios across the WS surface. Three concrete defects fixed: (1) `?botDifficulty=Easy|Medium|Hard|Master` was a black hole — captured into `AutotableConnection.BotDifficulty` (line 275) but never forwarded to the runtime. Every game played at Medium regardless of URL value, including spectator all-bots-watch URLs. (2) `ChangshaGameRuntime._strategy` was process-scoped (single field shared across every game on the host), so even if the plumbing had worked, setting one game to Hard would have flipped every other game. (3) `ResolveReplayEventSource` formatted `"bot:{_strategy.Difficulty}"` off the process-scoped strategy → audit replay always showed `bot:medium` no matter what the live game was running. Fix: new per-game `BotStrategy` field on `ChangshaGameInstance` (volatile, nullable; `?? _runtimeDefault` fallback keeps existing test harnesses unchanged); new `IChangshaGameRuntime.SetBotStrategyAsync(gameId, difficulty, ct)` + `GetActiveBotDifficulty(gameId)` accessor; `EnsureRuntimeBoundAsync` now plumbs `connection.BotDifficulty` through both the spectator auto-deal and human seat-take code paths; `ResolveReplayEventSource` takes the instance and uses per-game strategy so audit replay shows correct difficulty. Also fixed the long-standing `LateJoin_ToExistingGameId_ReceivesAccumulatedSnapshot_ForThatGameOnly` flake — Bob's payload used key `42L` which collides with the translator's synthetic face-down wall (`AutotableSlotMap.TotalTiles = 108` → keys 0..107 are reserved during Seating/RollingDice). Changed to `999_999_042L` + added a unique marker value so even a future translator change reintroducing wide-range synthetic ids can't pass. **New tests: `UrlBotDifficultyPlumbingTests` (8 cases — Theory over Easy/Medium/Hard/Master + case-insensitivity + unknown-falls-back-to-Medium + two-games-are-isolated). Playwright `playtest-bishop-bots.spec.mjs` (3 sections — 4-bot self-play soak, multi-game isolation across parallel contexts, late-join state delivery). Targeted `--filter "UrlBotDifficulty|MultiGameRouting|LateJoin|BotStrategy"` → 44/44 PASS in 10s. Broader sweep → 5324/5327 PASS (1 unrelated Vasquez W9 workflow YAML failure pre-existing on main; 2 intentionally-skipped 100-hand bot simulations). Playwright spec against dev backend → 3/3 sections PASS.** Three takeaways: (1) URL params need a plumb-AND-pin test pair — capturing a query param into `AutotableConnection.*` is *not* plumbing it, the integration path to the runtime is the load-bearing part. (2) Process-scoped engine fields are multi-game-poison — treat any `_field` on a singleton runtime as shared-state hazard; if there's per-game intuition, hoist it onto the per-game instance with `?? _runtimeDefault` fallback (audit candidates next sprint: ChangshaRuntimeOptions timeouts, the dice service). (3) Tile-id space (0..107) is a forbidden zone for synthetic collection keys in tests — the translator's face-down-wall synthesis re-uses that range when the runtime hasn't dealt yet; use `>= 1_000_000_000L` for test-side synthetic keys. Concurrent-agent gotcha: my edits to `AutotableWsEndpoint.cs` were silently clobbered by Frost's stash-pop ~28s after the `edit` tool returned success — mitigated by holding `.work/squad-git-lock` flock for the full edit→build→test→commit pipeline window (not just the git portion). **Decision memo:** `.squad/decisions/inbox/bishop-bots-multigame-audit.md`. **Squash commit on main:** see `git log --grep 'bot autonomy + difficulty'`.

📌 Team update (2026-06-03T17:00:00Z): Ripley L-10 follow-up — leave-seat WS broadcast. Closes the last MAJOR open item from Ripley's prodready-final audit (`.squad/decisions/inbox/ripley-prodready-final.md`). My prior `1febbd8` fix released the runtime seat correctly but `ReleaseSeatAsync` broadcasts on the SignalR `/hubs/changsha` channel — the autotable bundle listens on the WS `/autotable/ws` upstream protocol and never saw the cleanup. The pre-fix passthrough then stored `seats[playerId]={seat:null}` (a JSON object, NOT a tombstone) under the player's persistent key and echoed it to peers, and `nicks[playerId]` was never cleared. Net effect: every other client kept rendering the leaving player's ghost nickname at their old seat with the lobby counter stuck at 4/4; only a page refresh recovered the view. Fix mirrors `HandleDisconnectAsync` (same file, ~430 LOC below): the null branch of `TryHandleSeatTakeAsync` now calls `state.RemovePlayerEntries(connection.PlayerId)` after `ReleaseSeatAsync` and broadcasts the (seats|nicks|mouse)=null tombstones via `BroadcastToOthersAsync(..., full: false)`. Made the method return `Task<bool>` and updated the `case "seats"` arm in `ProcessUpdate` to skip the raw passthrough for the leave case so the `{seat:null}` payload isn't re-stored under the just-tombstoned key. ~30 LOC delta in `AutotableWsEndpoint.cs`; new acceptance test file `LeaveSeatBroadcastTombstoneTests.cs` (2 tests — tombstone is broadcast AND raw `{seat:null}` is NOT echoed); new Playwright spec `playtest-leave-seat-broadcast.spec.mjs` (9 gates, 2-browser scenario). Targeted backend filter `LeaveSeat|SeatTake|DisconnectSeatRelease` → 7/7 PASS in 4s. Broader autotable surface → 5326/5329 PASS (same 1 pre-existing Vasquez W9 YAML-regex flake + 2 deliberate bot-simulation skips). Playwright spec → 9/9 PASS, peer observes vacated seat in 20ms wall-clock with zero page errors in either browser. Two takeaways: (1) when asserting on per-player relay cleanup in a backend test, the raw WS client must first send the bundle's on-connect `["perPlayer", kind, true]` triples for seats/nicks/mouse — `RemovePlayerEntries` only iterates `_perPlayerKinds`, so without the declaration the cleanup is silently a no-op and the test fails for the wrong reason. The bundle declares these in `src/frontend/autotable-src/src/client.ts:131-134`. (2) Runtime-side state mutation + SignalR broadcast is not the same as autotable-WS broadcast — every state-changing runtime call that the bundle observes must also drive an explicit WS-layer broadcast (either tombstone delta or full-snapshot push via StateChanged). This is a recurring pattern that may warrant a follow-up sweep of other runtime APIs that mutate per-player relay-store entries (claim/pass, seat reseat under reconnect path, host promotion). **Decision memo:** `.squad/decisions/inbox/bishop-leave-seat-fix.md`. **Squash commit on main:** see `git log --grep 'broadcast seat tombstone on leave-seat'`.

📌 Bots autonomy + multi-game audit (2026-06-03): 3 defects fixed (botDifficulty URL, per-game strategy, replay audit) — committed `452b558`.

📌 Leave-seat WS broadcast fix (2026-06-03): Tombstone broadcast, 20ms latency proof — committed `35b7f76`.

📌 Team update (2026-06-04T14:35:00Z): W26 — Bot difficulty live differentiation PROOF. Stephen's directive after `452b558`: my plumbing fix correctly instantiates the right strategy class per game, but PROVE the tiers actually play differently in live games (not just that the class boundary changes). New spec `playtest-artifacts/playtest-bot-difficulty-live.spec.mjs` runs 12 spectator-watch 4-bot games (4 tiers × 3 trials) in parallel tier-contexts against `http://127.0.0.1:8088`, ~100s wall-clock end-to-end. Per trial taps CDP `Network.webSocketFrameReceived` for `["result","current",{...}]` entries (Frost's `4cd8963` proof pattern) and snapshots world for peak `meld` / `discard` counts. Captures outcome (Hu/Draw/Timeout), timeToHuMs, claimsAttempted, discardsCount, fans, fanPoints, basePoints, winResult.isSelfDraw, score deltas, page errors. **Verdict: DETECTED (2 spread signals, 0 page errors across all 12 games).** Headline numbers: Easy 0/3 Hu rate (all 3 trials drained the wall to ~54 discards and ended `type:"Draw"`), Master 3/3 Hu rate in median 14383 ms with peak 22 discards and just 12 claims. Easy → Master spread: −55.8% on time-to-Hu (32550 → 14383 ms), +100% on hu-rate (0.00 → 1.00). Medium/Hard each 3/3 Hu in ~19-20 s with claim counts 30/18 respectively — claim-count is NOT strictly monotonic because higher-tier bots filter out chi/pong opportunities that don't advance their goal hand (Master claims LEAST yet wins FASTEST, which is correct game-design behaviour — "smarter ≠ louder"). Brief's "Master = more claims" prediction needs to be updated to "more value-bearing claims" but that's a brief tweak, not a strategy bug. Fan-counts mostly zero (11 of 12 Hu's scored 0 fans) — matches Frost's `4cd8963` observation that a Standard 258-pair Hu on a claimed discard with no concealment / no self-draw / mixed suits legitimately scores no fan. Wire contract for `result.current` holds for Hu AND Draw across all 4 tiers; this proof widens Frost's single-tier `Hard` wire validation to the full 4-tier matrix. **Artifacts on main:** `playtest-artifacts/playtest-bot-difficulty-live.spec.mjs` (new spec, ~340 LOC), `playtest-artifacts/screenshots/bishop-bot-diff-2026-06-04T14-33-52-963Z/findings.json` (structured per Stephen's exact shape: `trials_per_tier`, `results.<tier>.{median_time_to_hu_ms, hu_rate, median_fans, median_claims, ...}`, `tier_differentiation`, `verdict`, `page_errors_total`), plus one screenshot per tier (`easy-final.png`, `medium-final.png`, `hard-final.png`, `master-final.png`). **Decision memo:** `.squad/decisions/inbox/bishop-bot-difficulty-live-proof.md`. **Squash commit on main:** see `git log --grep 'live difficulty differentiation'`. Two notes for future passes: (1) CWD-trap — when running a spec from inside `playtest-artifacts/`, a `join('playtest-artifacts', 'screenshots', ...)` artifact path resolves to `playtest-artifacts/playtest-artifacts/screenshots/...` — fixed by deriving the artifact root from `dirname(fileURLToPath(import.meta.url))`. Worth a sweep on the other ~20 specs in the directory. (2) For a higher-confidence ordering proof, future wave should add a `?seed=` URL param to the autotable bundle so wall RNG is identical across tiers within a trial pair — would let us assert per-trial pairwise `MasterTime < EasyTime` deterministically instead of relying on a 3-sample median.

📌 Production-Ready Wave (2026-06-04): Bot difficulty proof consolidated into squad decision (b5575b3). All gates go: Docker deploy proven, JWT restart-survival proven, bot tiers differentiated, game-flow complete.


### 2026-05-26 — Claim-window early-resolve (CanResolveEarly logic, SHA c7fdb8b)

Vasquez's postfix-verify found bots stalling during claim windows when Stephen (seat 0) had ANY claim opportunity, even if that opportunity could not beat an existing claim under standard Changsha priority + CCW tiebreak rules. The backend was blocking the full 5s timeout waiting for all eligible seats. Bishop added `CanResolveEarly(ChangshaGameInstance)` helper that compares unresponded seats' hypothetical max claims against the current leader's tier; if no unresponded seat could win, the window resolves immediately. Guarded against kong-robbing windows (those only contain Hu opps and must wait) and races with `ClaimTimeoutAsync` via null-check reentrancy.

**Key learning:** `CanResolveEarly` uses the exact same `TierOf(claim) + CounterClockwiseDistance` ordering as the claim-resolution path — ensuring that early-resolve never skips a winning seat. The bot turns can now fire in quick succession even when the human is idle, improving perceived responsiveness by ~30% (46–51 discards/60s post-fix vs 31–44 pre-fix in all-bots playtests).

### 2026-06-10 — Container health fix: ephemeral JWT signing key injection in CI (PR #96, `4a9c5e4`)
- Root cause: Dockerfile bakes `ASPNETCORE_ENVIRONMENT=Production`; Drake's prod-hardening throws `InvalidOperationException` if `Authentication:JwtSigningKeys` is empty. CI was not passing the key.
- Fix: Each of `e2e-playwright`, `multi-arch-runtime`, `multi-arch-smoke` now mints 48-byte ephemeral HMAC key via `openssl rand -base64` and injects as `-e Authentication__JwtSigningKeys__0=$KEY`.
- Verified: all three workflows now green; container boots in ~8s, `/health` reachable with all 4 fields.
- **Key learning:** When identical failures span local-working → CI-failing across 3 workflows, check `ASPNETCORE_ENVIRONMENT` divergence between `dotnet run` (defaults Development) and Dockerfile (defaults Production). Drake's prod-hardening is the trip wire.

📌 Team update (2026-07-27T01-56-23-811-07-00): You are the named revision owner for the #128 final-gate REJECT (Ripley) and own the P0 #139 backend fix: the #134/#135 claim fix (`8aaa37b`, main `ab3687d`) hangs human-vs-bots at hand 1 — the runtime human-claim / Chow-no-tileIds path wedges (`ChangshaGameRuntime` unchanged); main CI is RED. Your PR #133 (#132 result tombstone) and PR #135 (#134 claim wire-contract) were both approved. Continuation this spawn: #137 P0 post-Hu stall correction. — recorded by Scribe (decisions.md §2026-07-27).

📌 Team update (2026-07-27T01-56-23-811-07-00, late-arrival addendum): Two handoffs land in your lane: (1) **Frost's #136 verdict** — the recurring WS-discard-race flake now blocks PRs on the SqlServer cell; harden the WS-discard race window and/or serialize the WS-manual-deal acceptance family (`DbSerial`). (2) **Hudson's #137/#139 diagnosis** confirms you own the fix: hand 1 must not wedge when a human passes a bot Chow-no-tileIds claim; a real human Pung/Chow/Kong must advance to discard; a human Hu must finalize -> next hand. — recorded by Scribe.

📌 Team update (2026-07-27T02-51-38-764-07-00): Reiterated handoff from Frost's finalized #136 investigation: the recurring **WS-discard-race flake** is now visibly blocking PRs on the SqlServer db-providers cell — recommend hardening the WS-discard race window (queued-discard ack + `expectedVersion` resend, per your own audit memo) and/or serializing the WS-manual-deal acceptance family (`DbSerial`) or a bounded retry attribute. Not a persistence defect. — recorded by Scribe (decisions.md §2026-07-27).


📌 #153 backend fix — stale default-game retire (PR #156, `Refs #153`, 2026-07-27): Reproduced the user flow over real WS on pristine `507268f`. Confirmed Hudson's diagnosis (engine/scheduler healthy; defect = persistent `changsha-default` reattach). Initially prototyped a hand-1 bot-dealer auto-roll + WS pickup seat-ownership guard, then REVERTED both per Hudson's "do NOT invent a scheduler fix" + lane boundary. Final surgical fix (2 prod files, +106 LOC): `EnsureRuntimeBoundAsync(resettingConnection)` + `ShouldRetireStaleDefault` retire an abandoned, past-Seating default game for a seat-seeking newcomer via existing `RemoveGameAsync` (terminal row → hydration skips); new `IChangshaGameRuntime.TryGetSeatForPlayer` distinguishes deliberate reconnect (cookie `mahjong_pid`) from newcomer. Preserved: creator-wins + explicit-gameId join, reconnect, relay, spectator, live co-play. Tests `Issue153DefaultGameLifecycleTests` (4, real WS, primary RED on 507268f→GREEN). CI ALL GREEN incl. Test Sqlite/Postgres/SqlServer, e2e, playability-gate (Sqlite needed 1 rerun — pre-existing `EndHandResultBroadcastTests.AllBotFourHandGame` 90s-deadline flake, passes 3/3 local + Postgres+SqlServer in same run). Handoff to Ferro (frontend lane): dealMode default (auto vs manual), discard cue, unique-gameId minting. Decision memo: `.squad/decisions/inbox/Bishop-153-backend-fix-retire-stale-persistent-changsha-d.md`. Takeaway: the persistent `changsha-default` fallback is a shared-state hazard; retire it for newcomers on the seat-take path while keying reconnect off durable `ChangshaSeatState.PlayerId`, not transport `SeatConnections`.
