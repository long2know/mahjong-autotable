# Bishop - existing NEW producer contract for original Hicks

**The existing endpoint supports the narrow entry alignment; no additional NEW request property or producer edit is required for that consumer change.** Original Hicks d2f4f3f6 owns the frontend. His already-implemented entry-creation-03 manifest9b244d4d32d1f49d67150f41fbd5b02e356248813360666c19a2d5c1da8fb286 / entry69dad4da remains the separate held review input, not a relabel of approved baselineb237. This is producer-contract confirmation, not frontend/source/image approval.

## Exact supported exchange
1. Open the ordinary `/autotable/ws` connection with the intended fresh NAMED alias in the existing `gameId` query parameter, normal Changsha configuration and the same signed credential. Keep existing variant/dealMode/botCount/botDifficulty/seed/handCount/baseUnit/seat URL behavior. Seat remains a request hint, never ownership.
2. The first message for explicit creation is exactly `{"type":"NEW"}`. NEW does not carry its own gameId and requires no expectedVersion/requestId/createGame property. HandleNewAsync uses the connection's validated URL alias. Frontend-only createGame intent must stay frontend-only.
3. On success the ordinary acknowledgement is `{"type":"JOINED","gameId":"<public-alias>","playerId":"<signed-player-id>","isFirst":true-or-false}`. gameId here is the PUBLIC alias, not the opaque runtime gameId later advertised in private ownTurn/claim metadata. isFirst is neither seat ownership nor creation completion.
4. An ordinary UPDATE with full:true follows. A fresh unbound human table can initially have only the match placeholder and no runtime turn metadata. JOINED and that FULL must NOT consume creation intent or be treated as a completed game creation.
5. Normal client collection registration and the ordinary requested seat update drive binding. TryHandleSeatTakeAsync calls EnsureRuntimeBoundAsync with the captured explicit-new intent, real player/config, obtains the runtime's actual granted seat, applies configured bot-fill/start and sends a FULL per-viewer projection. SendFullSnapshotAsync explicitly serializes full:true and merges runtime ephemeral metadata, including turn. This is the existing producer for the consumer's connected/same-alias/bound-FULL confirmation. Runtime existence is separate from Hu/Kong availability; ownTurn can legitimately be null.
6. First creation commits the named binding and initial snapshot before publishing it; configuration is latched. Repeated NEW for the same NAMED target restores/reuses a known binding before considering creation. A later retry/reconnect URL cannot replace its seed/baseUnit/hand cap/difficulty/deal mode. The existing special stale-default-room behavior is not being redefined; the new consumer deliberately mints a non-default named alias.
7. A recovered known target is acknowledged with isFirst:false and its saved initial FULL before normal recovery scheduling resumes. Ordinary existing links and confirmed reload/reconnect remain JOIN. Relay paths remain unchanged.

These callsites were read directly in unchanged Endpoint752c673ad62b3aa09193d0bd94b9236622e3352a2a49d5b01d8405283c08697f: HandleNewAsync, TryHandleSeatTakeAsync, EnsureRuntimeBoundAsync, SendJoinedAsync and SendFullSnapshotAsync. Main runtime remains14b12539b672f3e0f45364e1dbcda5ab703d5c727c606255a206ae7553691fd2.

## Explicit failures and usable retry
Ambiguous ordinary JOIN stays fail-closed before JOINED. The sender gets `{"type":"UPDATE","entries":[["actionRejected","current",{"action":"room","reason":"legacy-room-binding-unavailable"}]],"full":false}` followed by close1011. This room rejection does NOT promise the live-socket corrective FULL used by ordinary claim/ownTurn errors. There are no requestedSeat/ownedSeat fields on this room envelope.

A real explicit New Game/Quick Match action can navigate/open a fresh connection with the SAME credential and intentional fresh target, then issue NEW. It must not send through the old closing/unbound socket, silently turn failed JOIN into NEW for the old alias, or mint replacement aliases during an unconfirmed same-target retry. Known missing/corrupt bindings fail closed even for NEW; no alias/seed/credential guess, backfill or deletion is permitted. The frontend marker may persist for same-target unconfirmed retry, and be consumed only on its actual bound FULL, as Hicks implemented. This source confirmation does not replace the requested real DB/WS lost-ack and browser reachability cases.

Hudson already has the NEW targeted signed legacy-owner/rejection/fresh-connection NEW/idempotency/nonlegacy/known-binding case matrix at sessions/2026-09-14-bishop-legacy-new-boundary.md (artifact2043fdfe). Final browser reachability remains required after independent entry review and candidate admission; no new live/cohort grant is implied.

## IMPORTANT current recovery-partial drift - no retag
During this confirmation, the shared working `ChangshaGameRuntime.PublicRooms.cs` changed from the frozen948237a651beabfde4d474e277776e2b68fe0fb209f8f9f026f87549251d2cbd to **0d70dc34ac6e2c1755707357717ecb474a5738863718075447d8ef48bcd198cb**. Bishop did not author, alter or functionally validate that edit. Read-only diff shows additional recovered-score fan and phase/continuation validation, NOT a change to NEW/JOIN signatures, explicit-new handling, alias lookup or binding publication.

The observed exact source is retained at `session-files/qualification/2026-09-12/bishop-actions/entry-producer-confirmation-01/observed-PublicRooms.cs`; its delta against frozen948 is `observed-recovery-delta.patch` SHA d46acce35e82dfa680ed00f8bd800d4b79fe55f3437839c9370c6b563624af54. This retention is NOT an implementation/review claim by Bishop.

Consequently e2c7e7b4/old948 execution seals and the earlier2043fdfe all27-match observation remain historical snapshots; they are not evidence for current0d70. A coherent CURRENT backend pin must incorporate the actual recovery revision and its own applicable review/result seal. No old95/269/55 results are transferred, no current compatible candidate is declared, and no source was reverted. The original Hicks entry producer contract can be coordinated without inventing new fields while that separate current-source gate is resolved.

## Explicit lost-ack confirmation for Hicks

For a present, valid, durably committed NAMED Changsha alias, repeated NEW restores/reuses the same runtime ID and creation-latched configuration; it does not call CreateGameAsync, redeal or create another row merely because an acknowledgement was lost. This was traced through HandleNewAsync -> RestoreRoomBindingAsync/CoreAsync and the named-target reuse branch in EnsureRuntimeBoundAsync. A recovered/known target has boolean isFirst:false and receives its authoritative FULL. Missing/corrupt known targets reject rather than become permission for a replacement.

This is NEW-operation idempotency, not a promise that normal game activity, timers or seat/disconnect/lifecycle operations freeze time or leave every connection field unchanged. The ordinary fresh-entry path does not opt a room into public-listing lobby destruction. Nothing in this confirmation relaxes preservation of the original unbound legacy rows in the requested adverse test.

JOINED alone is NOT creation completion. A fresh unbound match-only FULL is also insufficient. Hicks's same-alias, same-credential/config intent retained until a connected bound FULL with runtime turn metadata matches the existing producer; no implicit fallback or additional producer field is needed. This is source-level contract confirmation, not a new executed lost-ack test, independent approval, or transfer of frozen948 evidence to the observed newer recovery validation revision.
