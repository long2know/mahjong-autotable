# 2026-06-10T21-08-00Z — Ferro orchestration (PR #98)

**Routed by:** Coordinator (Scribe)
**Domain:** Frontend platform e2e (lobby, deep-linking, PWA, navigation races)
**Mode:** background
**Files authorized:** `src/frontend/autotable-src/src/lobby.ts`, `tests/e2e/*.spec.ts`

## Outcome
✅ **MERGED** as PR #98 (sha b1fccd2) on 2026-06-10T20:43:45Z

## Tests
- 15/15 pass (8 target + siblings)
- Duration: 1380s

## Key discovery
**Real source bug in lazy-mount race:** Playwright's `click()` fires `mouseenter` first, consuming `{once:true}` handler with `openOnLoad=false`; subsequent click calls `load()` which early-returns. Affected real users too (hover-then-click). Fixed via sticky `loadPromise` + synth-click in handler.

## Coordination notes
- Localized change to `scheduleSettingsDrawerLazyMount` function only
- Merges cleanly with Hicks's concurrent drawer edits
- No regressions introduced; all sibling specs green
