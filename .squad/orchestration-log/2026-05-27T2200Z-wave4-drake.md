# Wave 4 — Drake (Database hotfix agent)

**Timestamp:** 2026-05-27T22:00:00Z
**Mode:** background
**Branch:** `fix/playerstats-lastgameat-nullable`
**Squash SHA on main:** `c369c54`

## Outcome

**SHIPPED:** PlayerStats.LastGameAt schema mismatch hotfix.

**Problem:** `SqliteException: SQLite Error 19: 'NOT NULL constraint failed: PlayerStats.LastGameAt'` on fresh player profile creation.

**Root Cause:** Hand-rolled `CREATE TABLE` in `DatabaseBootstrapper.cs:301` had `LastGameAt TEXT NOT NULL DEFAULT '0001-01-01 00:00:00'` shadowing the EF model (which correctly declares nullable). Only surfaced on dev DBs where the file pre-dated Wave 5 (so `EnsureCreatedAsync` was a no-op).

**Fix:**
- Corrected the CREATE TABLE to `LastGameAt TEXT NULL`
- Added defensive remediation: introspect existing `PlayerStats` via `PRAGMA table_info`, rebuild if `notnull=1`
- Pattern: SQLite-only, idempotent, doesn't touch EF migrations (already correct on all 4 providers)

**Verification:**
- Build: 0 errors, 0 warnings
- Targeted tests: 11/11 passed (`PlayerStats`, `PlayerProfile`, `DatabaseBootstrap`)
- Full suite: **5219 passed, 4 flaky** (concurrent test-runner collision, repass 8/8 in isolation)
- Runtime smoke: Fresh DB + broken-DB remediation both OK

**Lane Discipline:**
- Only touched: `src/backend/src/Mahjong.Autotable.Api/Data/DatabaseBootstrapper.cs`
- Did NOT touch: EF migrations (correct on all providers), Changsha domain, frontend, workflows

---

**Coordination notes:**
- Completes before Bishop/Frost/Hicks/Vasquez can ship.
- EF migrations were already correct — bug was isolated to SQLite bootstrap.
