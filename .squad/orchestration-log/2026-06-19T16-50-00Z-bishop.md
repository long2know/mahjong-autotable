# 2026-06-19T16-50-00Z — Bishop orchestration (D1 backend rules regression)

**Routed by:** Coordinator (for Stephen / long2know)
**Domain:** D1 — backend rules-engine regression vs full canonical Changsha spec, on BOTH DB providers
**Mode:** background
**Model:** claude-opus-4.8 — max effort, long_context (1M)
**Worktree / base:** `reg/d1-backend-rules` from `main @ e5eb6f0`

## Outcome
- ✅ **VERIFICATION PASS — no PR** (no backend source touched). Changsha engine conforms to `docs/rules/changsha-spec.md` + `docs/specs/changsha-v1-contract.md` on SQLite AND Postgres.
- Full xUnit suite (Release, `--no-build`): **SQLite 5249/81/2** (11m44s), **Postgres 5249/81/2** (19m20s). The 81 failures are the IDENTICAL set on both providers (TRX diff).
- 0 real regressions; both watch-list flaky tests passed on both providers; Changsha namespace 551 passed + 2 intentional `[Fact(Skip)]`, 0 failures.
- 81 excluded = known environmental: Phase_K_W16/17/18 contract tests gate on `Directory.Exists(<dir>/.git)`; in a worktree `.git` is a FILE → locators return null. Pass in CI clones.

## Notes
- Correct Postgres config keys: `Persistence__Provider=Postgres` + **`ConnectionStrings__PostgreSql`** (NOT `__Postgres`). Postgres path genuinely exercised via per-process throwaway DB `mat_test_<pid>_<guid>`.
- Full criteria→tests→status table merged into `decisions.md` §2026-06-19.
