# 2026-06-19T16-50-00Z — Frost orchestration (D5 persistence/provider-swap)

**Routed by:** Coordinator (for Stephen / long2know)
**Domain:** D5 — persistence regression: SQLite↔Postgres provider swap, migrations, restart-hydration, prior-fault re-verify
**Mode:** background
**Model:** claude-opus-4.8 — max effort, long_context (1M)
**Worktree / base:** `reg/d5-persistence` from `main @ e5eb6f0`

## Outcome
- ✅ **VERIFICATION PASS — no PR** (no source change; 0 real regressions).
- **Provider swap:** SQLite default (`migrationsApplied=0`, EnsureCreated + bootstrap sweeps, 37 tables) ↔ Postgres via compose overlay (`Npgsql`, **`migrationsApplied=17`**, `MigrateAsync`). SAME codebase, no source change.
- **Restart-hydration** (`ChangshaGameRuntime.HydrateAsync`): non-terminal game (phase 9 AwaitingDiscard) → restart → "Hydrated 1"; terminal games correctly skipped.
- **Prior faults FIXED:** `PlayerStats.LastGameAt` now TEXT NULL (0 NOT-NULL failures); `PlayerProfiles.PlayerId` UNIQUE race-safe (24 concurrent same-PlayerId POSTs → 200, exactly 1 row; SQLite errno 19 / PG 23505 caught + retried).
- Persistence suites: SQLite **263/7**, Postgres **263/7** (7 = same MigrationW17Tests `.git`-worktree env issue).

## Notes
- Two env-only adjustments for the compose Postgres path (NEITHER a code/drift issue): buildx builder DNS (used `default` builder, restored after) and Production mandating `Authentication__JwtSigningKeys__0` (gitignored override). Documented overlay: `docker compose -f docker-compose.yml -f docker-compose.postgres.yml up -d --build`; key `ConnectionStrings__PostgreSql`.
