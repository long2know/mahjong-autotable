# Hudson — Orchestration Log (Phase 3 Wave)

**Date:** 2026-05-13 10:00 UTC  
**Agent:** Hudson (QA & Test Framework)  
**Branch:** `stlong/changsha-v1-phase3`  
**Commit:** `72e4ef4`  

## Task

Land frontend test infrastructure (vitest + jsdom + React Testing Library) with the first wave of reducer, bridge, and signalrClient contract tests.

## Outcome

✅ **SHIPPED** — Vitest infra complete, 47 frontend tests GREEN, build clean.

**Tooling added:**
- `vitest@^4.1.6`, `@vitest/ui@^4.1.6`, `jsdom@^29.1.1`, `@testing-library/react@^16.3.2`, `@testing-library/jest-dom@^6.9.1`, `@testing-library/user-event@^14.6.1`.

**Tests (47 total):**
1. `changshaReducer.test.ts` (19) — state transitions for GameCreated, PlayerSeated, GameStarted, TilesDealt, TileDiscarded, ClaimWindowOpen, ClaimMade, WinDeclared, ScoringComplete, BankerRotated, HandFinished, RoundChanged, GameEnded, reset.
2. `autotableBridge.test.ts` (5) — outbound queue, envelope format, inbound source validation, garbage-data drop, dispose cleanup.
3. `signalrClient.test.ts` (19) — all invoke wrappers (createGame, fillWithBots, takeSeat, startGame, discard, claim, reconnectGame, etc.) pinned to method + shape; event handler dispatch; teardown; ConnectionStatus mapping.
4. `useChangshaMockGame.test.ts` (4) — hook mount, dealMock split, discard removal, reset.

**Coverage gaps (documented for next wave):**
- `useLiveChangshaGame` (1.0d) — refactor needed; currently untestable (HubConnection in module scope).
- Component tests (1.0d) — ChangshaTablePage, DiceRollModal, PlayerHandPanel, BankerBadge, ClaimPromptModal, FanBreakdownPanel.
- `autotableBridge.diffAndSend` (0.5d).
- Reducer phase-machine invariants (0.5d).
- Tile utils pure helpers (0.2d).
- `useChangshaGame` mode picker (0.2d).
- Bridge security clobber-resistance (0.5d).

## Integration

- Tests pin state-transition correctness and wire-protocol contracts.
- No E2E coverage — live runtime vs. real .NET hub needs separate E2E spike.
- No CI workflow added; local `npm test` is the command.

## Next wave

Refactor `useLiveChangshaGame` to accept connection factory, then add lifecycle tests. Add component tests as Hicks stabilizes the UI. Consider adding `frontend-tests` job to `.github/workflows/squad-ci.yml`.

---

**Status in PR #25:** Merged as `72e4ef4`.
