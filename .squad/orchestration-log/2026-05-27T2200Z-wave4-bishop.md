# Wave 4 — Bishop (Backend state machine + translator)

**Timestamp:** 2026-05-27T22:00:00Z  
**Mode:** background  
**Branch:** `fix/walls-facedown-backend-translator-and-state-machine`  
**Squash SHA on main:** `9ca96c3`  
**Memo SHA on main:** `a86d425`

## Outcome

**SHIPPED:** Face-down walls (backend synthesis) + manual pickup ceremony state machine (acceptance tests).

**Problem (Stephen's directive):** Game start renders 108 face-UP tiles instead of face-DOWN walls; pickup ceremony is visually unauthored.

**Root Cause:** 
1. Bundle defaults to `DealType.HANDS` before WS connect, painting 14 face-up tiles to dealer.
2. In `Seating`/`RollingDice`, `state.Wall` is empty (not yet materialized), so translator emits 0 wall entries.
3. Bundle's local animation goes unauthored, exposing faces.

**Fix — Translator (`ChangshaToAutotableTranslator.BuildThingEntries`):**
- New gate: `ShouldSynthesizeWall(state)` → true iff `state.Wall.Count==0` AND `state.Phase ∈ {Seating, RollingDice}` AND no hands/discards/melds yet
- If true, synthesize 108 face-down wall tiles using canonical 14/14/13/13 `AutotableSlotMap` layout
- After `BeginManualDeal` materializes the shuffled wall, synthetic fallback shuts off

**Acceptance Tests (`ManualDealCeremonyTests.cs`):**
- 15 test cases pinning the translator + state machine:
  - Translator: 4 cases on face-down emission + slot layout
  - State machine: 7 cases on pickup round progression + cursor movement
  - Integration: 2 cases on wall shrinking during ceremony + auto-mode fast path
  - Non-regression: 1 case on EndHand without re-synthesis
- All green (144 ms)

**Verification:**
- Build: 0 errors, xUnit2002 warnings pre-existing
- Targeted tests: 15/15 passed
- Full suite: 5219 passed

**Lane Discipline:**
- Only touched: `ChangshaToAutotableTranslator.cs`, new `ManualDealCeremonyTests.cs`
- Did NOT touch: `Changsha/Dealing/**` (Frost), frontend, persistence, migrations

---

**Coordination notes:**
- Complements Hicks's frontend fix (`4d9e3ce`) — two layers of defense
- Hicks's frontend fallback is now redundant but safe (keeps privacy for late-joining spectators)
- Branch collision warning: Feature branch now `fix/walls-facedown-backend-translator-and-state-machine` to avoid clobbering Hicks's `fix/walls-facedown-and-pickup-choreography`
- Memo: Requests optional WS smoke test (manual dump of first `things` snapshot)
