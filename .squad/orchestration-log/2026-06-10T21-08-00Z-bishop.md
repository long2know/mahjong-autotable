# 2026-06-10T21-08-00Z — Bishop orchestration (PR #99)

**Routed by:** Coordinator (Scribe)
**Domain:** Backend API e2e (auth, OAuth, JWKS, ELO/leaderboard)
**Mode:** background
**Files authorized:** `src/backend/src/Mahjong.Autotable.Api/Auth/AuthTokenController.cs`, `src/backend/src/Mahjong.Autotable.Api/Auth/AuthController.cs`, `src/frontend/autotable-src/src/auth.ts`, `src/frontend/autotable-src/src/leaderboard.ts`, `tests/e2e/*.spec.ts`

## Outcome
✅ **MERGED** as PR #99 (sha 693ca79) on 2026-06-10T21:01Z

## Tests
- 7/7 target specs + 9/9 verification specs pass
- Duration: 2476s
- Backend suite: 5261/5343 pass (93 pre-existing `FindRepoRoot` failures, identical on baseline)

## Key discoveries

### Production bug #1: OAuth provider envelope drift
Backend returns `{id, displayName, enabled, kind}` objects; frontend assumed legacy `["google", …]` string array. Every OAuth button silently hidden in real deploys. Fixed by teaching consumer to accept both shapes.

### Production bug #2: ELO fallback banner unreachable
`fetchOnce` on 404 flips `state.mode = 'stats'` BEFORE render runs; render guard `mode === 'rating' && ratingsAvailable === false` becomes dead code. Fixed by gating on `ratingsAvailable === false` alone.

## Coordination notes
- Did NOT touch JwtSigningKeyProvider.cs (Production guard + eager-key check from #96 intact)
- `/api/auth/providers` shape change is front-end-tolerant (will accept either form in future)
- Re-pinned JWKS Cache-Control from `public, max-age=60` to `no-store` per e2e contract + Phase L requirements
