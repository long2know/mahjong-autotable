# Orchestration Log — Hicks

**Timestamp:** 2026-05-19T17:00Z
**Agent:** Hicks (Frontend Lead)
**Mode:** background (parallel with Vasquez + Bishop)
**Model:** claude-opus-4.7-xhigh
**Elapsed:** ~23 min
**Requested by:** Stephen Long
**Branch:** `stlong/phase-f-changsha-realism`
**Phase:** Phase F — Wave 4 (parallel)
**Parallel-batch siblings:** Vasquez (tests), Bishop (backend)

## Task

Ship the Phase F frontend: variant switching (restore upstream Riichi 4p / 3p / Bamboo / Minefield alongside Changsha), manual-pickup state-machine UI (dice button, wall-click intercept, pickup HUD), deal-mode + bot-count + bot-difficulty pickers, variant indicator, URL-param + localStorage support.

## Why chosen

Hicks owns the autotable bundle (`src/frontend/autotable-src/`); the Phase B `GameType` collapse he authored must now be partially reverted with a clean toggle layer; manual-pickup UI sits in the same files he touched for Wave 3's dice HUD + claim arc.

## Why this mode

Background — fully disjoint from Vasquez/Bishop per Ripley §6 scope table. Hicks's files (TS bundle, HTML, CSS, parcel artifacts) never overlap with Bishop's backend dir or Vasquez's test dir. Hicks consumes Ripley §2.4's wire contract (`pickup` collection schema) without waiting for Bishop's backend to ship — the bundle gracefully renders nothing when the channel is empty.

## Input artifacts read

- `.squad/decisions/inbox/ripley-phase-f-design.md` — design contract
- `.squad/agents/hicks/history.md`
- `git show 98d4cca^:src/frontend/autotable-src/src/setup-slots.ts` — pre-Phase-B `SLOT_GROUPS` recovery
- `git show 98d4cca^:src/frontend/autotable-src/src/setup-deal.ts` — pre-Phase-B `DEALS` + `POINTS`
- `git show 98d4cca^:src/frontend/autotable-src/src/types.ts` — pre-Phase-B `GameType` enum
- `src/frontend/autotable-src/src/{types,client,world,setup,setup-deal,setup-slots,game-ui,things}.ts`
- `src/frontend/autotable-src/index.html`, `src/style.css`

## Output artifacts produced (production frontend)

- `src/frontend/autotable-src/src/types.ts` — restored `GameType` (CHANGSHA + 4 upstream); extended `Conditions` with `back`/`fives`/`points`/`baseUnit`/`dealMode`; `Conditions.defaultsFor(gameType)` factory; new `PickupEntry` type
- `src/frontend/autotable-src/src/setup-slots.ts` — upstream `SLOT_GROUPS` + START slots restored verbatim
- `src/frontend/autotable-src/src/setup-deal.ts` — upstream `DEALS` (incl. FOUR_PLAYER's 11 roll-conditional `HANDS` variants) + `POINTS` table
- `src/frontend/autotable-src/src/setup.ts` — branched `setup`/`addTiles`/`tileIndex`/`replace`/`getScores`/`addSticks` on `gameType === CHANGSHA`
- `src/frontend/autotable-src/src/client.ts` — `pickup` collection registration (`Collection<string|number, PickupEntry>`)
- `src/frontend/autotable-src/src/world.ts` — pickup state, `onPickup()`, `isMyPickupTurn()`, wall-click intercept (no optimistic move), `toggleHonba`, `resetPoints`, deal overrides
- `src/frontend/autotable-src/src/game-ui.ts` — variant + dealMode + bot + fives + points pickers; pickup HUD; roll-dice button; variant badge; URL params; localStorage (`autotable.phaseF.v1.*` namespace; `?bots=true` aliases `?botCount=3`)
- `src/frontend/autotable-src/index.html` — picker DOM scaffold + pickup HUD + roll-dice button + variant badge + break marker
- `src/frontend/autotable-src/src/style.css` — Phase F styles (variant body class drives picker visibility)
- `src/frontend/autotable/` — parcel build artifacts (bundle `autotable-src.6d5fae4c.js`)
- `.squad/decisions/inbox/hicks-phase-f-frontend.md` — frontend delta record

## Outcome

**Completed.** `tsc` strict ✓; parcel build ✓ (1.03 MB bundle). Live click-through pending Bishop's pickup runtime — until then the bundle stays silent on pickup channels and Wave 3 features behave identically.

Wave 3 backward-compat preserved: `?variant=changsha&dealMode=auto` → no pickup HUD, no roll-dice button, fast-deal path identical to Wave 3.

## Key design decisions

1. **Strict lockout of frontend-only pickup state.** Bundle never mirrors Bishop's pickup phase machine — it only renders what arrives. Optimistic UI explicitly rejected; "take" click does NOT pre-move tiles.
2. **`wallTileIds` is informational.** Frontend computes by name-sort walk; backend may use for cross-validation or ignore.
3. **Variant hot-swap deferred to Phase G.** Reload warning instead.
4. **Dual-affordance:** Take-N HUD button AND wall-click intercept emit the same `pickup.take`.

## Bishop dependencies stubbed (acceptable — bundle renders nothing when channel is empty)

- Backend `pickup` collection emit/ack
- Backend bot engine (count + difficulty pickers persist locally but don't yet flow on the wire)
- Runtime variant gating for non-Changsha (legacy upstream relay path works locally)
