# Bishop — Orchestration Log (Phase 3 Wave)

**Date:** 2026-05-13 10:00 UTC
**Agent:** Bishop (Backend Dev)
**Branch:** `stlong/changsha-v1-phase3`
**Commit:** `c306874`

## Task

Implement five surgical backend fixes addressing conformance bugs from the Phase 2 audit, incorporate Vasquez's v1.2 banker rotation lock, and land 203 passing tests.

## Outcome

✅ **SHIPPED** — All five fixes implemented, 0 failed tests, build clean.

**Fixes:**
1. **Banker rotation canonical** — rewrote `RotateBanker` to make winner the next dealer; washout keeps dealer's seat.
2. **Kong/Pung same-tier priority** — extracted `ChangshaClaimPriority.cs` as single source of truth; both runtime and adjudicator call `TierOf(claim)`.
3. **Per-hand wall seed mixing** — `RotateRandomSeed(state.Seed, state.HandNumber)` ensures 16 different walls per game.
4. **Chow tile IDs honored** — new `ResolveClaim` overload validates chosen tiles (sequential, same suit, in hand); fallback to lowest pattern if null.
5. **Missed-win enforcement** — `MissedWinSeats` tracking; flagged seats blocked from `Hu` only (self-draw still allowed).

**Test delta:** 179 → 203 (net +24; banker rewritten +5, wall seed +4, priority +5, chow tileIds +6, missed-win +5).

**Files touched:** 7 source, 6 test (1 rewritten, 2 new, 3 updated).

## Integration

- Frontend impact: new error code `CHOW_TILES_INVALID` surfaced on bad chow submission; `BankerRotated.reason` uses new string constants.
- Persistence: still deferred (snapshots not rehydrated on process restart).
- E2E coverage: still deferred (only one-hand E2E test exists; 16-hand E2E remains the best regression guard).

## Next wave

- `DiceService` seed mixing (visual-only impact).
- Persistence hydration on restart.
- 16-hand E2E test to lock FIX-1 + FIX-3 at the hub layer.

---

**Status in PR #25:** Merged as `c306874`.
