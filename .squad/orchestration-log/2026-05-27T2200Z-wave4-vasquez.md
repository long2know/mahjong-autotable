# Wave 4 — Vasquez (Visual gate spec + red-baseline playtest)

**Timestamp:** 2026-05-27T22:00:00Z
**Mode:** background
**Branch:** `test/visual-gate-walls-facedown`
**Status:** NOT MERGED (red-baseline) — branch origin `950c2565` (squash on main deferred)

## Outcome

**SHIPPED (red-baseline):** 6-gate spec `playtest-artifacts/playtest-changsha-dealing-ceremony.spec.mjs` designed to gate Wave 4 deliverables.

**Test Gates:**
1. **GATE-1: wallCount** — Expect ≥100 walls after initial paint
2. **GATE-2: allWallBackRotation** — All wall tiles at `rotationIndex=0` (face-down)
3. **GATE-3: zeroForeignHandFaceUp** — No other-seat hands visible (privacy preserved)
4. **GATE-4: fourSeatWalls** — Four canonical walls, one per seat
5. **GATE-5: pickupReachedDealerHand** — Dealer hand grows from 9→14 over ~3.5s (ceremony drives pickups)
6. **GATE-6: pageErrorsCount** — Zero console errors

**Baseline Status:**
- Designed against baseline SHA `c616407` (pre-Wave 4)
- Expected to FAIL gates 1, 2, 4, 5 on baseline (known bugs)
- Will EXIT 0 after Bishop's fix verifies (gates all pass)
- Then can be merged to main

**Implementation:**
- Polling loop: measure `window.game.world.things` state every 500ms for 5 seconds
- Per-gate assertions: count wall tiles, check `rotationIndex`, validate seat distribution
- Test runs against `E2E_BASE_URL=http://127.0.0.1:8088`
- Spec at `playtest-artifacts/playtest-changsha-dealing-ceremony.spec.mjs`

**Verification Strategy:**
- Baseline run: gates fail (expected) ✓
- Post-Bishop-fix run: gates all pass ✓
- Will transition to merged green once Bishop's work is validated

**Lane Discipline:**
- Touched: New test spec file only
- Did NOT touch: backend, frontend code, migrations

---

**Coordination notes:**
- Vasquez owns the gates, not the implementation fixes
- Spec is deliberately red-baseline to prove the bugs exist
- Merge happens after Bishop/Frost/Hicks pass their gates
- Validates visual correctness independently from unit tests
