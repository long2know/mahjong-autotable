# Hicks — Orchestration Log (Phase 3 Wave)

**Date:** 2026-05-13 10:00 UTC  
**Agent:** Hicks (Frontend Dev)  
**Branch:** `stlong/changsha-v1-phase3`  
**Commit:** `f6c298e` + `cb00885`  

## Task

Ship the Lobby + Claim UX so users can browse `/changsha`, click "Play vs Bots", and play a real hand against three bots via SignalR.

## Outcome

✅ **SHIPPED** — Lobby + claim UX complete, 48/48 vitest tests green, build clean.

**Features:**
1. **Lobby flow** — `LobbyCard` with player-name input, "Play vs Bots" button → `createGame` → `takeSeat` → `fillWithBots` → `startGame`.
2. **Hand UX** — sorted hand rendering, concealed/added kong buttons, self-draw (Zimo!) button, chow tile-combo picker.
3. **Claim resolution** — per-seat discard trays, claim opportunities with priority order, chow tileIds forwarded to backend.
4. **Cross-cutting fixes** — SignalR positional-args bug (critical), `reconnectGame` aliased to dedicated hub method, `FullState` reducer case, `phaseFromWire` normalization.

**Test coverage:** 48 tests across 4 files (reducer, bridge, signalrClient, mock hook).

**Files touched:** 14 files; +1248 / −209 net.

## Deferred to v3.1+

- Real 3D mesh rendering in autotable iframe (atlas textures, mesh).
- Bidirectional canvas → hub events.
- Mid-hand reconnect animation replay.
- Server-supplied chow combo hints.

## Next wave

- Hook refactor to allow `useLiveChangshaGame` testing in isolation (currently untestable; HubConnection in module scope).
- Component tests for DiceRollModal, PlayerHandPanel, ClaimPromptModal, FanBreakdownPanel, BankerBadge.
- Bridge `diffAndSend` testing.
- Reducer phase-machine property tests.

---

**Status in PR #25:** Merged as `f6c298e` + `cb00885`.
