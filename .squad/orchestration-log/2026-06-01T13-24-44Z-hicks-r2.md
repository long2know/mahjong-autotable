# Orchestration Log Entry

### 2026-06-01T13:24:44Z — Hicks round 2 (broken-deal cleanup)

| Field | Value |
|-------|-------|
| **Agent routed** | Hicks (Frontend / Three.js specialist) |
| **Why chosen** | Three secondary visual bugs in Stephen's `broken-deal-repro-2026-06-01T20-05-35-522Z.png` screenshot (gray corner wedges, center HUD, wall gap) all originating in frontend `setup-slots.ts` / `object-view.ts` per-variant logic. Hicks owns autotable renderer. |
| **Mode** | `background` |
| **Why this mode** | Parallel with Frost's diagnostic (no input data dependency); Hicks's lane fully isolated from backend; diagnostics already available from Vasquez/Frost prior sprint. |
| **Files authorized to read** | `.squad/decisions/inbox/` (Frost's memo); `src/frontend/autotable-src/src/{setup-slots,object-view,world}.ts`; prior Hicks round-1 notes. |
| **File(s) agent must produce** | Commits to `src/frontend/autotable-src/src/{setup-slots,object-view,world}.ts` + rebuilt bundle; diagnostic memo `.squad/decisions/inbox/hicks-cleanup-round2.md`. |
| **Outcome** | **Completed.** Commit `b4c82ec`: Killed corner wedges (per-seat wall rows 14/13 in `setup-slots.ts`), center HUD hidden for Changsha via `object-view.ts::setVariant()`, wall gap eliminated (phantom slot drop shadows removed). Three playtests → zero page errors. Visual proof: `playtest-artifacts/screenshots/hicks-deal-fixed-round2-20260601T202305Z.png`. |

---

## Notes

- **Per-seat wall geometry:** `setup-slots.ts` now allocates `row(14)` for seats 0/1 and `row(13)` for seats 2/3 (matches backend's 28/28/26/26 = 108 layout), replacing the uniform `row(19)` that was leaking 4–6 phantom columns per seat.
- **Variant toggle:** New `setVariant(gameType)` in `object-view.ts` hides tray meshes + center scoreboard for Changsha, shows them for Riichi variants. Constructor reads URL variant at first paint.
- **Bundle rebuild:** Parcel re-bundled cleanly; size stable within rounding error.
- **Residual:** Top-wall corner "gap" is physically correct (corner unowned by any seat). Flagged as design decision for Stephen (accept-as-physics or re-origin walls in Phase-G).
