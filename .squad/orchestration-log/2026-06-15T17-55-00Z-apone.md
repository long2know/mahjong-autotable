# 2026-06-15T17-55-00Z — Apone orchestration (PR #101 + #102)

**Routed by:** Coordinator (Scribe)
**Domain:** Supply-chain CI / SLSA action-pinning (`slsa-drift-detection`)
**Mode:** background
**Model:** claude-opus-4.8 — max effort, long_context (1M)
**Files authorized:** `.github/workflows/*.yml` (slsa-drift-detection scanner + pinned refs across helm-release + squad-* workflows)

## Outcome
- ✅ **MERGED** PR #101 (`9435742`) — 10 tag-drifted action refs SHA-pinned across 6 workflows; drift 10→0; dispatched run 27586054167 = GREEN.
- ✅ **MERGED** PR #102 (`7a399b5`) — closed the scanner blind spot: pinned 12 inline-list `- uses:` refs + broadened scanner regex; dispatched run 27586513750 = GREEN.

## Key discovery
The `slsa-drift-detection` scanner regex `^[[:space:]]*uses:` matched ONLY the multi-line `name:`/`uses:` step form, so 12 genuinely-unpinned `- uses: actions/checkout@v4` (inline-list form) across 11 squad workflows were invisible. ORDER MATTERS — pinned all 12 to `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2` FIRST, then broadened the regex to `^[[:space:]]*-?[[:space:]]*uses:[[:space:]]+[^[:space:]#]+` (broadening before pinning would have turned the scanner red).

## Convention established (team-wide, durable)
EVERY `uses:` ref — BOTH the `name:`/`uses:` step form AND the inline-list `- uses:` form — MUST be pinned to a 40-char commit SHA with a trailing `# vX.Y.Z` comment. The drift scanner now enforces both shapes; never ship `@vN` / `@branch` / `@tag`.

## Coordination notes
- #102 is the follow-up #101 explicitly flagged; fully resolves the inline-list blind spot.
- Allow-list (`slsa-framework/slsa-github-generator/*` reusable + local `./`) preserved exactly; pins: checkout `# v4.2.2`, azure/setup-helm `# v4.3.1`, cosign-installer `# v3.7.0`, github-script `# v7.0.1`.
