# 2026-06-15T17-55-00Z — Hicks orchestration (PR #104)

**Routed by:** Coordinator (Scribe)
**Domain:** Frontend Production-CSP inline-style cascade + 10 independent render/gating bugs
**Mode:** background
**Model:** claude-opus-4.8 — max effort, long_context (1M)
**Files authorized:** `auth.ts`, `style.css`, `lobby.ts`, `game-ui.ts`, `index.html`, `tests/e2e/*.spec.ts`

## Outcome
✅ **MERGED** PR #104 (`d92bab2`) — 11/11 e2e green vs own Production-CSP backend on :8091 (`ASPNETCORE_ENVIRONMENT=Production` + JWT key + strict CSP).

## Key discoveries
- **Only 1 of 11 was truly CSP-caused:** `auth.ts` innerHTML templates carried `style="display:none"` (hash `sha256-aqNN…UAE=` == `display:none`), blocked by `style-src-attr` → seed hidden state via CSSOM (`el.style.display='none'`) after appendChild; CSP NOT weakened, post-fix violations = 0.
- **Dev-vs-Prod CSP divergence (MAJOR):** `appsettings.Production.json` `Security:CspStrictStyles=true` → `SecurityHeadersMiddleware.DropStyleUnsafeInline` → `style-src 'self'`. Inline styles work in Dev (`'unsafe-inline'` kept) but are BLOCKED in the deployed Docker container. Repro against a Production-env backend, NOT Dev.
- **The other 10 were independent pre-existing bugs:** signin-modal close interception (backdrop z-index over card), seat-preview mount-gating (only on game-scene mount), mobile move-log-toggle a11y over-reach from #100's `body.lobby-active`, replay `Collection.on()` not replaying current entry (also fixed reconnect-into-finished-game), spectator-chat `?seat=-1` with no gameId, tournament ×5 lazy-mount + duplicate `tournament-register-btn` testid → detail renamed `tournament-detail-register-btn`.

## Conventions established (durable)
- Prefer CSSOM (`el.style.display`/`cssText`) or static CSS classes over inline `style=` / `<style>` / `setAttribute('style')` under strict CSP.
- Unique-testid contract (`selectors.md`): no duplicate testids across list-row quick-action and static detail button.

## Coordination notes
- Rebuilt + committed served dist. Regression-checked CSP/a11y/lobby/auth/settings/profile/tournament/bracket — green. Visual-regression (Ferro #103, picked up on rebase) re-verified green with the now-visible lobby seat-preview.
