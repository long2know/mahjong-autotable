# Orchestration Log — Bishop Phase G (2026-05-20T20-30-58Z)

**Agent:** Bishop (Backend Dev)
**Mode:** background
**Branch:** `stlong/phase-g-bot-scheduler-lobby`
**Commit:** a22f10c

## Task routed
Phase G backend — bot pickup tick scheduler + privacy-mask slot-parse fix.

## Why chosen
Bishop owns the Changsha runtime, autotable WS endpoint, and the privacy-filter contract. Two production issues required his scope: (1) bots freeze during manual-deal pickup phases (no ScheduleBotIfNeededAsync yet wired for pickup), (2) FilterEntriesForViewer pre-existing slot-parse bug (extracting seat from between `.` and `@` instead of after `@`).

## Files authorized
- `src/backend/src/Mahjong.Autotable.Api/Changsha/Runtime/ChangshaGameRuntime.cs`
- `src/backend/src/Mahjong.Autotable.Api/Autotable/AutotableWsEndpoint.cs`

## Files produced
**Production:**
- `ChangshaGameRuntime.cs`: ScheduleBotIfNeededAsync extended with pickup branch; new private RunBotPickupAsync; RollDiceAsync and TakeTilesFromWallAsync now invoke ScheduleBotIfNeededAsync.
- `AutotableWsEndpoint.cs`: FilterEntriesForViewer re-parses slot at last `@` (not between `.` and `@`); XML doc rewritten.

**Test (via Vasquez parallel):**
- 11 facts, 60 assertions across BotPickupSchedulerAcceptanceTests (6 facts) + PrivacyMaskAcceptanceTests (5 facts).

## Verification
- `dotnet build ... --nologo` → 0 warnings, 0 errors, ~6s.
- `dotnet test ... --nologo --no-build` → **330/0/9/339** (+11 facts via Vasquez's tests).
- No flakes across 3 consecutive runs.

## Outcome
✅ Shipped. Both contracts stable; tests locked.
