# Orchestration Log — Ripley

**Timestamp:** 2026-05-19T16:30Z
**Agent:** Ripley (Lead, Architect)
**Mode:** sync (gating Vasquez/Hicks/Bishop fan-out)
**Model:** claude-opus-4.7-xhigh
**Elapsed:** ~16 min
**Requested by:** Stephen Long
**Branch:** `stlong/phase-f-changsha-realism` (cut from `stlong/phase-b-changsha-scene`)
**Phase:** Phase F — Changsha Realism (Wave 4 design gate)

## Task

Design the Phase F architecture in response to Stephen's 2026-05-19T16:05Z directive: manual pickup (default for Changsha), variant switching (restore Riichi 4p/3p/Bamboo/Minefield), 0/3/4 bot fill modes, 3-tier Changsha bot engine. Produce a disjoint-scope design contract so Vasquez/Hicks/Bishop can fan out in parallel without file-level collision.

## Why chosen

Architecture decision with cross-cutting impact (state machine + protocol + frontend + backend + tests). Lead-only call. Required upstream-Phase-B revert + new collection kind + runtime-vs-relay gate — all of which need a single coherent design before any agent writes code.

## Why this mode

Sync — Vasquez/Hicks/Bishop are blocked on the §6 scope table (file ownership) and the §2.4 wire shape (the `pickup` collection schema). Stephen also needed to confirm bot-difficulty scope (ship all three tiers vs Medium-only) before Bishop could lay out the engine; Ripley flagged it and Stephen accepted "ship all three."

## Input artifacts read

- `.squad/decisions/inbox/copilot-directive-2026-05-19T1605Z-changsha-realism.md` — Stephen's directive
- `.squad/decisions.md` (Wave 3 prior art — runtime-vs-relay precedence, per-viewer privacy filter, `ChangshaPhase` state machine)
- `.squad/agents/ripley/history.md` — prior Phase A pivot context
- `docs/rules/changsha-spec.md` v1.2 — dice + break-point + pickup ceremony
- `src/backend/src/Mahjong.Autotable.Api/Changsha/Runtime/ChangshaGameRuntime.cs` — Wave 3 deal flow
- `src/backend/src/Mahjong.Autotable.Api/Autotable/ChangshaToAutotableTranslator.cs` — translator output surface
- `src/backend/src/Mahjong.Autotable.Api/Autotable/AutotableWsEndpoint.cs` — relay path
- `src/frontend/autotable-src/src/setup.ts` / `setup-deal.ts` / `setup-slots.ts` / `types.ts` / `client.ts` / `world.ts` / `game-ui.ts`
- Pre-Phase-B `git show 98d4cca^:src/frontend/autotable-src/src/setup-slots.ts` (recovery point for restored `SLOT_GROUPS`)

## Output artifacts produced

- `.squad/decisions/inbox/ripley-phase-f-design.md` (~955 lines)
  - §0 Executive summary
  - §1 Variant-switching architecture (Relay vs ChangshaRuntime)
  - §2 Manual-pickup state machine (6 new phases, `pickup` collection schema, wire shape)
  - §3 Deal-mode toggle (`manual` default for Changsha, `auto` for upstream)
  - §4 Bot fill modes (0/3/4)
  - §5 Full Changsha bot engine (`IChangshaBotStrategy`, Easy/Medium/Hard)
  - §6 **File-scope map for Vasquez/Hicks/Bishop (DISJOINT)** — the parallel-fan-out enabler
  - §7 Backward compat / risk
  - §8 Exit criteria (5 named smoke tests, test gates, DOD)
  - §9 Sign-off summary

## Outcome

**Completed.** Design contract landed; Vasquez/Hicks/Bishop unblocked to fan out in parallel from this single document. Wave 4 launched immediately on completion.

Key architectural keystones articulated:
1. Runtime-vs-relay duality already exists from Wave 3 — Phase F adds one switch (`AutotableConnection.RuntimeMode`), no new dual-code-path.
2. Manual pickup is a sub-state machine grafted on `ChangshaPhase.Dealing`, not a parallel system.
3. Wave 3's per-viewer privacy filter handles partial-hand privacy correctly — zero new privacy code.
4. One new collection kind: `pickup` (singleton; key=`current`/`0`).

Locked defaults per Stephen's "accept defaults": Easy/Medium/Hard all ship; Changsha default; manual default for Changsha + auto for everything else; botCount=3; botDifficulty=Medium; bot delays 800/500/400 ms.

§6 disjoint scope table prevented file-level collision during the Wave 4 parallel landing (verified: zero merge conflicts on the 3-agent simultaneous push).

## Self-critique

Left a corrective breadcrumb in §2.4 (initial mislabeling of `PickupRound3` as count 1, with correction below) as a "trust but verify" hint for Bishop. `FOUR_PLAYER_DEMO` drop documented as deliberate divergence rather than oversight. The translator's hardcoded `gameType="FOUR_PLAYER"` in `BuildMatch` was flagged as a Wave 3 lie Bishop needed to fix when restoring variant authority.
