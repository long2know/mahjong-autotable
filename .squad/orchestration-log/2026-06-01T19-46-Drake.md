# Orchestration Log Entry

---

### 2026-06-01T19:46Z — PlayerProfiles.PlayerId UNIQUE race-safe upsert

| Field | Value |
|-------|-------|
| **Agent routed** | Drake (Persistence Hotfix Engineer) |
| **Why chosen** | Stephen's live-play crash: `UNIQUE constraint failed: PlayerProfiles.PlayerId` — classic concurrent-request race in `GetOrCreateAsync`. Drake owns race-safe persistence patterns. |
| **Mode** | `background` |
| **Why this mode** | No hard data dependencies; Drake can autonomously fix the SELECT-then-INSERT race and validate against replay simulation. |
| **Files authorized to read** | `.squad/decisions/inbox/copilot-directive-20260601T194608Z.md`, existing PlayerProfileService tests |
| **File(s) agent must produce** | `src/backend/src/Mahjong.Autotable.Api/Players/PlayerProfileService.cs` (fix), regression test in PlayerProfileServiceTests.cs, `.squad/decisions/inbox/drake-playerprofiles-unique-fix.md` (decision memo) |
| **Outcome** | Completed — commit `2df2e75` (upsert with cross-provider error handling + 2-attempt retry loop). Follow-up memo `5144dc8` (docs). 20 parallel `/api/identity` probes all succeed; exactly 1 row lands in each of PlayerProfiles + PlayerStats. |

---
