# Orchestration Log — Bishop

**Timestamp:** 2026-05-19T17:00Z
**Agent:** Bishop (Backend)
**Mode:** background (parallel with Vasquez + Hicks)
**Model:** claude-opus-4.7-xhigh
**Elapsed:** ~38 min (bottleneck of the Wave 4 fan-out per Ripley's §6 prediction)
**Requested by:** Stephen Long
**Branch:** `stlong/phase-f-changsha-realism`
**Phase:** Phase F — Wave 4 (parallel)
**Parallel-batch siblings:** Vasquez (tests — Bishop's red→green target), Hicks (frontend — Bishop's wire-contract consumer)

## Task

Ship the Phase F backend: `DealMode` + 6 new pickup phases on `ChangshaPhase`; `Pickup` collection kind + `PickupEntry`/`BreakPointWire` wire types; `BeginManualDeal` + `TakeTilesFromWall` state-machine entry points; `AutotableRuntimeMode` gate (Relay vs ChangshaRuntime) parsed from query params; 3-tier `IChangshaBotStrategy` engine (Easy/Medium/Hard) + `HandEvaluator` + `ChangshaBotEngine`; translator emit of `pickup["current"]`; runtime `RollDiceAsync` + `TakeTilesFromWallAsync` calls under `instance.Lock`.

## Why chosen

Bishop owns the backend (`src/backend/src/Mahjong.Autotable.Api/`); Phase F backend touches 12+ files including a new `Changsha/Bot/` directory with 6 new files for the strategy engine. Ripley §6 explicitly flagged Bishop as the Wave 4 bottleneck.

## Why this mode

Background — fully disjoint from Hicks/Vasquez per Ripley §6 scope table. Bishop reads Vasquez's failing-red acceptance tests as the implementation target (red→green) without waiting for completion — the reflection-based test scaffolding means his progress is observable through test-pass count.

## Input artifacts read

- `.squad/decisions/inbox/ripley-phase-f-design.md` — design contract (the 955-line backend playbook)
- `.squad/decisions/inbox/vasquez-phase-f-rule-audit.md` — rule audit + falsifiable bot specs
- `src/backend/tests/Mahjong.Autotable.Api.Tests/Changsha/Acceptance/ManualPickupAcceptanceTests.cs` (23 cases)
- `src/backend/tests/Mahjong.Autotable.Api.Tests/Changsha/Acceptance/VariantSwitchAcceptanceTests.cs` (~15 cases)
- `src/backend/tests/Mahjong.Autotable.Api.Tests/Changsha/Acceptance/BotEngineAcceptanceTests.cs` (22 cases)
- `.squad/agents/bishop/history.md`
- `src/backend/src/Mahjong.Autotable.Api/Autotable/{AutotableProtocol,AutotableWsEndpoint,ChangshaToAutotableTranslator}.cs`
- `src/backend/src/Mahjong.Autotable.Api/Changsha/{ChangshaDomain,ChangshaStateMachine,ChangshaBotPolicy,BreakPointService,DiceService}.cs`
- `src/backend/src/Mahjong.Autotable.Api/Changsha/Runtime/{ChangshaGameRuntime,ChangshaRuntimeOptions}.cs`

## Output artifacts produced (production only — `src/backend/src/Mahjong.Autotable.Api/**`)

- `Autotable/AutotableProtocol.cs` — new `Pickup` collection kind + `PickupEntry`/`BreakPointWire` value classes
- `Autotable/AutotableWsEndpoint.cs` — `AutotableRuntimeMode` enum (`Relay` | `ChangshaRuntime`); `AutotableConnection` init props (`Variant`, `DealMode`, `BotCount`, `BotDifficulty`, computed `RuntimeMode`); query-param parsing with defaults; `HandleUpdateAsync` branching; `TryHandlePickupActionAsync`
- `Autotable/ChangshaToAutotableTranslator.cs` — emits `pickup["current"]` during `IsPickupPhase(state.Phase)`; tombstoned on `AwaitingDiscard`
- `Changsha/ChangshaDomain.cs` — `DealMode` enum (`Auto`|`Manual`); 6 new `ChangshaPhase` values (`BreakPointMarked`, `PickupRound1/2/3`, `SingleTilePickup`, `DealerExtra`); state fields (`BreakPoint`, `PickupSeatIndex`, `PickupExpectedCount`)
- `Changsha/ChangshaStateMachine.cs` — `BeginManualDeal(state, DiceRoll)`, `TakeTilesFromWall(state, seat, count)` (invariant-checked, advances cursor via `AdvancePickupCursor`), `ExpectedPickupCount`, `IsPickupPhase`
- `Changsha/ChangshaBotPolicy.cs` — thin facade over `ChangshaBotEngine.Resolve("medium").DecideAction(state, seat)` (legacy harness unchanged)
- `Changsha/Bot/IChangshaBotStrategy.cs` — interface (4 hooks + unified `DecideAction` router)
- `Changsha/Bot/EasyStrategy.cs` — highest-rank discard, never Chow, always Hu, Kong only when hand ≥10
- `Changsha/Bot/MediumStrategy.cs` — port of legacy `ChangshaBotPolicy` keep-score (pairs + adjacencies + 2/5/8 bias)
- `Changsha/Bot/HardStrategy.cs` — Medium + defensive-discard penalty against `state.DiscardPile`; EV-gated Kong (`CountLooseTiles ≥ 2`); shanten-improving Chow only
- `Changsha/Bot/HandEvaluator.cs` — `SelectDiscardTile`, `CountLooseTiles`, `FindConcealedKongCandidate`, `FindAddedKongCandidate`, `CollectDiscardedLogicals`, `MinShantenToHu` (coarse)
- `Changsha/Bot/ChangshaBotEngine.cs` — case-insensitive resolver; null/unknown → Medium; singleton instances
- `Changsha/Runtime/ChangshaGameRuntime.cs` — `RollDiceAsync(gameId, seat, ct)` + `TakeTilesFromWallAsync(gameId, seat, count, ct)` under `instance.Lock`; `StartGameAsync` branches on `DealMode`
- `Changsha/Runtime/ChangshaRuntimeOptions.cs` — `BotPickupDelayMs = 500`
- `src/backend/tests/Mahjong.Autotable.Api.Tests/Mahjong.Autotable.Api.Tests.csproj` — global usings to accept Vasquez's reflection-heavy test file unchanged (build-infra only)
- `.squad/decisions/inbox/bishop-phase-f-backend.md` — backend delta record

## Outcome

**Completed.** `dotnet build src/backend/Mahjong.Autotable.slnx --nologo` → 0/0. `dotnet test src/backend/Mahjong.Autotable.slnx --nologo --no-build` → **318 passed / 1 failed / 9 skipped of 328 total** at Bishop's hand-off. The single failure was diagnosed in this commit as a **test bug** (not production): `ManualPickupAcceptanceTests.Pickup_PrivacyMask_OpposingHandsHaveFacesStripped` parses the slot format `"hand.{handIdx}@{seat}"` from the wrong side. Bishop documented the fix recommendation (`slot.EndsWith("@0")` instead of `slot.StartsWith("hand.0")`) but **did not fix it from his seat** — file-scope discipline (tests are Vasquez's scope).

All 22/22 bot engine acceptance tests, all variant switch tests, 22/23 manual pickup tests passing.

## Crucial state-machine detail recorded

The FIRST pickup IS the dealer's round-1 of 4 tiles. Phase stays at `BreakPointMarked` until that first call lands, then advances to `PickupRound1` for the next 3 seats. `ExpectedPickupCount`: 4 for `BreakPointMarked` + `PickupRound1/2/3`, 1 for `SingleTilePickup`, 1 for `DealerExtra`.

## Test bugs surfaced (filed for follow-up, NOT fixed from Bishop's seat)

1. `ManualPickupAcceptanceTests.cs` slot-suffix parsing bug (the single failing test). 5-minute test edit; Vasquez's scope.
2. **Pre-existing bug in `FilterEntriesForViewer` (AutotableWsEndpoint.cs:644-652).** Parses the slot text the same wrong way. **Non-blocking** (no test exercises it before Phase F; relay-mode connections never invoke the filter; runtime privacy path uses translator output directly). Deferred maintenance.

## Deferred follow-ups (filed)

- **Bot pickup tick scheduler** — `OnPickupCue` hook is in place and returns `Wait` per the test contract; runtime tick loop that schedules `TakeTilesFromWallAsync(gameId, pickupSeat, ExpectedPickupCount(phase))` after `BotPickupDelayMs` is NOT yet wired. Small `Task.Delay` loop in `ChangshaGameRuntime`; not added in this commit because none of Vasquez's tests exercise it and adding it risks racing with the existing turn-tick loop. Needed before Hicks's frontend goes live.
- **`FilterEntriesForViewer` slot-parse cleanup.**
- **`MinShantenToHu`** — coarse approximation; rigorous shanten counter deferred to V2.

## Open Phase F items punted to Hicks (frontend follow-on)

- Break-point gem render
- Active-pickup highlight per seat
- Per-seat tile slice animations
- Roll-dice button gate on `dealMode=manual` + `phase=RollingDice`
- Bot pickup auto-take tick (driven by `BotPickupDelayMs`)
- Variant select with `dealMode` toggle

(All of these are in Hicks's `hicks-phase-f-frontend.md` delivered in the same Wave 4 batch — disjoint scope held.)
