# Phase 5a Shipped — Session Summary
**Timestamp:** 2026-05-13T22:06:33Z

## What Shipped

**Phase 5a: 3D Renderer Realization (Strategy C)** — PR #26 squash-merged at `ce1bda6`.

Autotable WS server now exposed at `/autotable/ws`, byte-identical upstream bundle (`autotable.9519e86d.js`) connects and renders live Changsha game state in 3D. Walls (14/14/13/13 split), hands (own seat face-up, others face-down), discards, and melds (concealed kongs face-down) all visible.

**Backend:** AutotableProtocol, AutotableSlotMap, ChangshaToAutotableTranslator, AutotableWsEndpoint. Wall split 14/14/13/13 enforced per Default #6. `fives='000'` forces clean tileId → thing-index mapping. Tests 203 → 226 (+23).

**Frontend:** Iframe URL `/autotable/?gameId={id}&embedded=1&seat={N}` with embedded-mode sidebar hiding, camera-toggle HUD button, postMessage bridge. `useMemo([gameId, userSeat])` prevents reload. Tests 48 → 60 active.

**Tests:** 12 new vitest cases covering iframe URL, camera-toggle message, embedded-mode fixture, reducer regression guard. 2 intentionally skipped (manual / fallback). No regressions.

## Defaults Locked (Accepted)
All 8 Hicks-recommended defaults: auto-roll, sandbox preserved, camera toggle, 4-side radial discards, snap reconnect, 14/14/13/13 wall, `/autotable/ws` path, throttling deferred to 5d.

## Known Limitations (Phase 5b+)
- 28 wind/dragon things visible in bundle (Phase 5c optimization)
- Bundle UPDATEs discarded (Phase 5b: translate to hub commands)
- One game per backend instance (Phase 5d: multiplexing)
- `isFirst: false` hard-coded (Phase 5b: re-evaluate)

## Deviations Documented
- Wall split followed explicit Default #6 lock (spike text variant noted)
- Reflection-based `JsonSerializer` matches codebase convention (not source-gen)

## Next: Phase 5b
Add inbound UPDATE translation path for bundle mouse drag → hub commands. Coordinate via AutotableWsEndpoint debug-log location.
