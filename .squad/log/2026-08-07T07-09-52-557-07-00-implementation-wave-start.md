# Session — Implementation wave START (UAT-fix, exact image 200cad4)

- **Began:** 2026-08-07T07:09:52.557-07:00 (after Ripley locked contracts)
- **State backend:** local (`FSStorageProvider`) · **Team root:** `/data/source/mahjong-autotable/.squad`
- **Continues:** the 2026-08-07 full-team UAT wave (§2026-08-07 in `decisions.md`). Prior 200cad4 completion remains **SUPERSEDED**.

## Lanes (isolated worktrees at 200cad4)
- **Bishop** — `/tmp/mahjong-autotable-uat-backend` — wire / seat / claim / input / **privacy** / wall (BE-1..BE-5 + F-3 opaque per-viewer ids).
- **Hicks** — `/tmp/mahjong-autotable-uat-frontend` — mode / input / disconnect / variant / geometry (FE-1..FE-4, FE-6, RC-4 wall render, RC-13).
- **Ferro** — `/tmp/mahjong-autotable-uat-ui` — chrome / dropdowns / responsive (UI-1, FE-5, FE-6/G20).
- **Hudson** — `/tmp/mahjong-autotable-uat-tests` — **G4–G21 / P1–P5 RED matrix** at exact image 200cad4 (real-UI, exact-state + fresh-state).
- **Roles:** Ralph monitors; Vasquez rules oracle (R-1); Frost reviewer (RV-1 + RV-2 security sign-off); Ripley integrator (Lead; no production edits).

## Locked contracts (recorded in `decisions.md` §2026-08-07)
Single architectural fault: client-authoritative relay Setup/Deal/geometry colliding with the server-authoritative ChangshaRuntime, plus a translator that hardcodes `FOUR_PLAYER/fives=000`. Root causes RC-1..RC-13; contracts BE-1..5, FE-1..6, UI-1, R-1, H-1, RV-1; **BLOCKING F-3 privacy** (wire `things` key==tileId leaks every hidden hand + wall order → opaque per-viewer ids; G19/P-1..P-5). RED matrix G1–G21.

## t=0 baseline (Ralph board)
- **Exact-image divergence hypothesis CLOSED** — served bundle `272e68a2` == source@200cad4; the defects are **real code**, not a stale bundle.
- 4 worktrees empty (0 code landed); board = contract-readiness, not progress.
- **Backend is the critical-path bottleneck** (BE-1/BE-3/BE-5/BE-4 gate the FE side).
- **3 co-design sub-contracts to lock before parallel edits:** G19/F-3 wire format (BLOCKING), RC-4 wall geometry, RC-13 dealMode source.
- Flags: UI-1 + Hudson RED can start now; **R-1 (Vasquez) is an unstarted hard prerequisite**; premature-green tripwire (rebuilt bundle + sharpened gates); **G19 is the BLOCKING closure gate (supersedes §9).**

## R-1 addendum + progress (2026-08-07T08:0x)
- **R-1 DELIVERED (Vasquez, BINDING):** manual ceremony = one click on the authoritative front endpoint takes the whole batch (4 in BreakPointMarked/Rounds1-3, 1 in SingleTile/DealerExtra; dealer→CCW; totals dealer 14 / others 13 / wall 55); post-deal normal draws auto from FRONT, Kong replacements auto from BACK, wall inert otherwise; **Auto ⇒ wall always inert**; discard/claim mutually exclusive. Recorded in `decisions.md` §2026-08-07 R-1 block. **Unblocks FE-2/G17 + FE-5.**
- **Ripley reconciliation (D-1..D-5):** because SC-2 opaque per-viewer wall keys, the **client pickup match is `targetSlots`, not `targetTileIds`** (targetTileIds per-viewer projected, validation only); tombstones `EncodePickupCleared()`/`EncodeClaimWindowClosed()` exist but are uncalled → wire up (D-3); F1 wall-frame mismatch = engine dealer-relative [14,13,14,13] vs render seat-absolute [28,28,26,26] (D-4); F2 intra-stack → G4 stack-level set-membership (D-5). Bishop adds `targetSlots[]`+`targetTileIds[]` to `PickupEntry`.
- **Also merged/archived this turn:** SC-1/SC-2/SC-3 frozen wire sub-contracts (Ripley); G19 security oracle P-1..P-5 + reject thresholds (Frost); Hudson input-boundary addendum.
- **Implementation progress:** 3/4 lanes moving (uncommitted WIP, file-disjoint) — Bishop BE-1/2/3/5 (translator + WsEndpoint + TDD), **Hicks FE-1..FE-5 landed RED→GREEN** (49 tests, tsc clean — `world.deal` inert in Changsha kills the four-half-walls scatter; input allowlist; disconnect banner-park; atomic variant badge), Ferro UI-1 `color-scheme` + relay/Riichi DOM removal. **FLAG: Hudson RED lane 0-dirty ~15 min** — the RED baseline gates every lane's GREEN; coordinator to confirm dispatch. No commits/builds/greens yet.

## Scribe actions this turn
- Recorded the implementation-wave START + a consolidated LOCKED-contracts block in `decisions.md` (§2026-08-07).
- Merged the 13 UAT-evidence inbox memos (Ripley synthesis, Bishop audit, Frost ×5, Ralph ×4, Hudson ×2) — **archived verbatim (non-lossy) to `decisions-archive.md`** and deleted from the inbox (12→0, then a 13th impl-board file also swept). Live-log carries the consolidated decisions; full memos live in the archive.
- Wrote this session log + per-lane/role orchestration entries.
- **Local state only — no commit / branch switch / production edits.** `decisions.md` is 54,675 B (over the 51,200 soft ceiling; all content is 2026-08-05/06/07 so nothing is age-eligible for archival yet — a consolidation turn or the 7-day gate will trim once 2026-08-05/06 age out).

## G19 oracle formalization + tests-split (2026-08-07T08:1x)
- **G19 security oracle + correction fully recorded** (`decisions.md` §2026-08-07 G19 block, enhanced): P-1..P-5 each BLOCKING; hidden tiles use **explicit entitled-only identity** (real id only to entitled viewers) **+ opaque non-inferable handles**; **strict inbound entitlement/field filtering** (server rejects actions on handles a viewer wasn't issued; pairs with BE-2); **live raw-WS T1–T10** + T6′/T6″/T11 + P-1..P-5 against the REAL backend. Correction (binding): seated/rejoin handles **stable across reconnect for the game lifetime** (durable `PlayerId`, not per-connection fresh); different viewers **unlinkable**; **≥64-bit STRING** handles (avoid JS ≥2^53 precision loss); **no client-known brute-forceable derivation** (server-secret HMAC over the 108-tile universe). Reject thresholds R1–R13.
- **Tests-lane split (Ripley)** resolves the Hudson RED bottleneck: hudson-2 (schema-independent gates) ∥ hudson-1 (schema-dependent G4/G15/G17/G19/P1-P5 + GameComplete); disjoint spec/helper ownership; quarantines the invalid wall proxies.

## SC-4 FINAL — pickup = targetHandles (2026-08-07T08:2x)
- **Pickup designation FINALIZED as `targetHandles:string[]`** (the picker-viewer's opaque projected `things` keys for Wall[0..count-1], co-derived one pass); position/marker fields display-only; outbound `pickup.take` sends handles, server-resolved under the connection namespace (rejects foreign/non-designated/wrong-endpoint/phase/seat); no raw wall ids; reconnect-stable (P4), cross-viewer unlinkable (P3), explicit clear tombstone. **Vasquez withdrew the slot alternative; Frost approved handles** (raw ids/slots-with-identity fail G19 P1). Settles the slots↔handles churn (D-2 → handle-corr → ripley-FINAL-slots → CANONICAL-slots → **SC-4 handles FINAL**).
- **Supersedes** the intermediate `ripley-CANONICAL-key-lock` (slots). **LIVE REALIGNMENT:** Hicks had built the client against slots + Bishop hadn't emitted the field ⇒ an interim over-broad fallback (not G17-satisfying) is live; Hicks must switch to `hovered.key ∈ targetHandles` and Bishop co-emit `targetHandles[]` before FE-2/G17 can GREEN. Recorded in `decisions.md` §2026-08-07 (SC-4 block + corrected R-1 wire-signal bullet).

## SC-4 pickup key CONTESTED — do NOT finalize targetHandles (2026-08-07T08:25)
- Coordinator directive: **do not finalize the SC-4 `targetHandles` entry.** A **Lead slot-based canonical convergence (user-exact endpoint)** is pending Ripley's confirmation: `pickup.targetSlots` len-1 = co-derived Wall[0] trigger; server `count` (4/1) owns the whole `Wall[0..count-1]` batch; **only the trigger is actionable, other batch tiles inert**; inbound carries NO trusted identity/slot/handle; server validates phase/seat/count; no raw ids; explicit clear. **Supersede targetHandles + multi-target-actionable language ONLY once Ripley confirms.**
- **Unresolved Ripley signal conflict:** `ripley-SC4-pickup-schema-IMMUTABLE` locks targetHandles, but **Frost withdrew his targetHandles-for-pickup advice and CONFIRMED the count-based/slot model G19-SAFE** (C1–C4: targetSlots display-only public position; drop raw `wallTileIds`; single-trigger gating; server trusts no inbound tile ref). Opaque handles remain OUTBOUND-projection-only for `things` privacy (G19 unchanged). Scribe holds both; targetHandles marked NON-FINAL; recorded in `decisions.md` §2026-08-07 (SC-4 block ⚠️ + 08-25 pending-convergence block).

## SC-4 pickup key — FINAL STATE THIS TURN: UNRESOLVED / oscillating (08:2x–08:29)
Correction to the note above: the key oscillated again. Signals within minutes: **(A) `targetHandles`** (Ripley IMMUTABLE freeze + Frost RV-2 concur) vs **(B) slot-based single-trigger `targetSlots` length-1 Wall[0]** (coordinator 08:25 directive + **Ralph 08:29 "SC-4 stricter latest"**). **Neither implemented** (Ralph 08:29: both lanes still on the any-wall fallback, which the stricter SC-4 forbids). Per the coordinator directive, **Scribe does NOT finalize** either — targetHandles stays non-final; supersede only on a definitive Ripley confirmation of (B). Count-based take (drop raw `wallTileIds`, server trusts no inbound tile ref) is common to both. Recorded in `decisions.md` §2026-08-07 (SC-4 ⚠️ + 08-25 + 08-29 status blocks). Also: Ralph VOIDed the tests-split (canonical Hudson lane = `/data/source/...-uat-tests`, 12 `red-*.spec.ts`).

## SC-4 CONVERGED (08:33) — resolving this turn's oscillation
Final state this turn: the pickup key **converged to a SYNTHESIS** (Ripley FINAL/IMMUTABLE, per Ralph 08:33): **`targetHandles:string[]` length EXACTLY 1** = opaque per-viewer `things` key of the reachable **Wall[0] TOP tile** (single-trigger — multi-target batch model superseded); `targetSlots` display-only; no raw ids; `hovered.key ∈ targetHandles`; count-based take. This reconciles both poles (opaque-handle key + single-trigger user-exact endpoint) and honors the coordinator's single-trigger/no-multi-target intent. Implementation OPEN: Bishop not started (blocks G17+G19); Hicks partial (delete the residual `world.ts:546-549` targetSlots slot-match); Hudson partial (re-key `red-wall-input` to the opaque-handle top-trigger). Recorded in `decisions.md` §2026-08-07 (SC-4 ✅ CONVERGED + 08-33 alignment block).

## SC-4 pickup key — SPLIT-BRAIN → RESOLVED to (B) single-trigger-slot (08:39–08:40)
Ripley (ripley-2) sent a direct 08:39 reconciliation asking me to record SC-4 = `targetHandles` (whole Wall[0..count-1] batch) as sole authority. **Conflict:** a PARENT-LOCKED `ripley-SC4-FINAL-single-trigger-slot` file VOIDS the targetHandles SC-4 (enforces the user's original single-exact-trigger requirement: `targetSlots` len-1 Wall[0], NO handles, take `{seatIndex,count}`), **Frost RV-2 FINAL (08:40)** endorses single-trigger-slot (superseding his targetHandles concurrence), and **Hicks shipped it** (83 tests). I recorded **(B) single-trigger-slot as canonical**, marked Ripley's 08:39 targetHandles request **SUPERSEDED** (cited the older IMMUTABLE file; predates the parent-lock/RV-2), and **replied to ripley-2** flagging the conflict + asking it to confirm/re-broadcast. Also recorded Vasquez's withdrawal of her targetSlots-batch endorsement + "pick one key" flag. Recorded in `decisions.md` §2026-08-07 (08-39 block + updated §351/SC-4 markers). Did NOT record targetHandles as "sole authority" — would contradict the parent-lock + RV-2 + shipped code.

## SC-4 pickup key — coordinator's 08:49 targetHandles instruction CONTESTED by shipped (B)-slot; recorded ground truth + escalated
The 08:49 task directed me to record Ripley FINAL/IMMUTABLE = `targetHandles` len-1 + supersede slot-key text. I recorded the instruction but did **NOT** assert it as sole authority — it conflicts with the team's shipped/reviewed canonical: **Ralph 08:48 CANONICAL CORRECTION** (targetHandles superseded; canonical = (B) single-trigger-SLOT), **Hicks shipped (B)** (targetSlots len-1 slot-name, 83 tests, holding exact-1), **Bishop reconciling to (B)**, **Frost RV-2 SUPERSESSION** (targetSlots canonical, targetHandles superseded for pickup). Recorded **(B) `targetSlots` len-1 (reachable top-layer Wall[0], F2) as ground-truth canonical** + the 08:49 targetHandles instruction as contested/unresolved; **escalated to ripley-2** for a single confirmation. Consolidated the 08-1x churn block + marked 08-25/29/33/39/4x superseded (decisions.md 88.2KB→~87KB). Did NOT switch the record to targetHandles-sole-authority over the shipped (B) code — would misrepresent ground truth.

## Ferro UI lane outcome (08:58) — landed, not complete
Recorded Ferro's UI-lane deliverables (worktree `/tmp/mahjong-autotable-uat-ui`, uncommitted): **UI-1** production dropdown auto-enhances the real `select.dark-select`/`settings-v2-select` (real `#game-type` opened-popup screenshot/contrast/value-change/idempotence/fallback RED→GREEN); **FE-1** DOM relay/Riichi default-hide + **FE-6** responsive landed; UI suite **13 pass / 14 mobile skip**, ESLint+strict tsc clean. **NOT complete** — pending Ripley integration + the canonical **Hudson exact-image close** (real Changsha-visible selects on the integrated build, not `#game-type`). Recorded in `decisions.md` §2026-08-07 (08-58 block). No commit.

## SC-4 pickup key — DEFINITIVELY CLOSED to `targetSlots` (08:57); my escalation vindicated
Ripley's **08:57 DEFINITIVE** ("Settled by CODE + PARENT. Not reopening") + the coordinator's **08:52 v4 FINAL** confirm the pickup match key = **`pickup.targetSlots`** (public slot, len-1, top-first reachable Wall[0]); **`targetHandles` is RETIRED from pickup permanently.** Ripley owned the root confusion (he'd labeled two schemas "immutable SC-4" — an early handle version + the parent's slot version). Key clarification: **two distinct `string[]` fields** — pickup `targetSlots` (public slot, the gate) vs SC-2 `things` opaque per-viewer handles (privacy/rendering only, never the pickup key). This **vindicates the (B)-ground-truth record + escalation** I held instead of asserting the coordinator's targetHandles instruction as sole authority. Marked the 08-49 targetHandles block VOID/RETIRED; §351 + markers → DEFINITIVE (B). Also recorded Ralph's escalation: Bishop backend STARVED (pickup v4/F2/SC-2 all 0-movement) → recommend Ripley split F2-slot-map + SC-2-helper sub-lanes; leave the v4 pickup rename as a Bishop nudge. Recorded in `decisions.md` §2026-08-07 (08-57 + 08-58 blocks).

## SC-4 pickup key — Ripley 09:02 targetHandles reversal CROSSED/held; canonical stays targetSlots
ripley-2 (09:02) re-asserted `targetHandles` as sole pickup key — directly contradicting its OWN 08:59 DEFINITIVE ("targetSlots, targetHandles retired, Not reopening") from minutes earlier, and the shipped code. **Hicks HOLD (09:01):** pickup stays on `targetSlots` (0 targetHandles residue, 89 tests; notes handle-matching would BLOCK FE-2 via unshipped plumbing). I did NOT flip the canonical — recorded the 09:02 message as a crossed/superseded reversal and **held canonical at `pickup.targetSlots`** (08:59 DEFINITIVE + shipped code + Frost RV-2 + Ralph v4). Per Hicks, reopening to handles needs an explicit supersession of the 08:59 DEFINITIVE + Frost RV-2 re-sign — not issued. **Escalated to ripley-2** flagging the self-contradiction. Recorded in `decisions.md` §2026-08-07 (09-02 block).

## SC-4 v4 FINAL re-confirmed (09:11) + backend split trigger MET
Recorded the versioned **SC-4 v4 FINAL** directive (09:11) by making the 09-06 canonical heading explicit ("SC-4 **v4** FINAL", re-confirmed 09:11) — dedup, not a new block; the block already carried the full contract (targetSlots len-1 reachable/top Wall[0] + count; no handles/tileIds; exact-slot fail-closed; take seat+count; server validates+removes front batch; clears wired; SC-2 opaque handles separate; the monotonic "any different pickup key in any delivery order is < v4 and disregarded" rule; all prior memos superseded). Also updated the 09-0x backend-status block: **Ralph's split trigger is MET** (Bishop 0-hit on SC-2/SC-4/F2 across 3 intervals) → recommend Ripley execute the two split sub-lanes now; FE-2/G17 HOLD stays open.

---

### 2026-08-07T09:21 — SC-4 pickup key: CLOSED / CONVERGED (Ripley closure)

Ripley confirmed convergence closure for the manual-pickup match key. Frost's FINAL memo
(`Frost-RV2-FINAL-SC4-single-trigger-slot.md`) now ALIGNS with the parent-locked DEFINITIVE
ruling — pickup match key = `pickup.targetSlots` (single-trigger, top-first, len-1) — superseding
his OWN prior "count-based targetSlots confirm" AND "targetHandles pin". The question is now
consistent across ALL parties:

- **code** — Hicks/Bishop slot-based (`targetSlots`, 89 tests, 0 `targetHandles` residue)
- **parent** — locked `targetSlots`
- **rules** — Vasquez invariants
- **review** — Frost RV-2 FINAL (aligned)

No dissent remains. Recorded as **CLOSED/CONVERGED** on decisions.md §2026-08-07T09-06
(SC-4 v4 FINAL / IMMUTABLE block; enhanced in place — no duplicate block).

**Separately OPEN / BLOCKING (do NOT conflate):** the SC-2 opaque per-viewer HANDLE projection
for hidden `things` (G19 / F-3 privacy) is Bishop's lane and remains unshipped — the ONLY place
opaque `targetHandles`-style handles belong. Two distinct `string[]` fields, both recorded:
pickup = `targetSlots` (public slot, settled); SC-2 `things` = opaque handles (privacy, open).

Cross-agent: appended a closure note to Frost's history; folded Frost history Waves N & O
long-form into `history-archive.md` (history.md 18,647 → 12,237 B, back under the 15,360 gate,
non-lossy). Scribe: no commit, no branch switch, no production edits.

### 2026-08-07T09:20–09:26 — Operational wave: F2 golden tightened, split fired/producing, 2 new blockers, Hicks HOLD

Processed 4 inbox memos (Ralph ×3, Hicks ×1) via dedup-into-existing-threads + one new dated block each where warranted:

- **Ralph 09:20** — F2 golden criterion TIGHTENED → BLOCKING: must prove, ∀ 52 dealer×sum × EVERY ceremony phase, that the current `Wall[0]` up-link is EMPTY (dynamic depletion). Initial/layer-only goldens REJECTED; `StuckTurnWallAndDiscardSignalTests.cs` (#160) is the superseded ordinal proof — must NOT be relabeled. Armed split still unfired ~9 min past MET trigger (compounding stall; Hicks/Hudson blocked). Recorded on the 09-1x F2 line + 09-0x split thread.
- **Hicks HOLD (09:25)** — declined a 09:23 `targetHandles` pickup reopen (pivot ~14, re-citing the < v4 SUPERSEDED `ripley-SC4-pickup-schema-IMMUTABLE.md`); HELD on `targetSlots` (102 contract tests, tsc clean). First live test of the 09:21 closure's "reopening requires explicit supersession" rule — reopen DISREGARDED per SC-4 v4-FROZEN. Also technically: `hovered.thing.key` is numeric today (SC-2 unshipped) ⇒ handle gate would fail-closed. Recorded on the 09-21 closure block; cross-agent note appended to Hicks history (folded pre-07-27 detail → archive, 18,986 → 15,344 B, non-lossy).
- **Ralph 09:23** — armed split FIRED (`-uat-backend-slotmap`, t=0); NEW-1 (manual bot-dealer strands human non-dealer / stall — Bishop+Hicks+Hudson) + NEW-2 (remove client auto-take, require real endpoint click — Hicks+Hudson); D1 must-stay-GREEN, D4 must-stay-RED until mode-boundary fix; g17_hold = 12 rows. New 09-23 block.
- **Ralph 09:26** — split topology = 4 worktrees, NO file overlap; bishop-2 `-backend-helpers` PRODUCING (F2 top-first flip DONE `AutotableSlotMap.cs:169` `(seat,o/2,1-(o%2))`; SC-2 `OpaqueTileHandleProvider` = HMAC-SHA256 opaque per-viewer string). GAP 1 = its F2 golden is layer-only ⇒ REJECTED (dynamic 52×phase golden still owed); GAP 2 = bishop-1 hasn't consumed provider + top-first `targetSlots` yet (integration seam, watch); orphan `-uat-backend-slotmap` to retire. New 09-26 block; supersedes the 09-23 "SC-2 not yet spawned" flag.

All 5 raw memos archived verbatim to `decisions-archive.md`. Scribe: no commit, no branch switch, no production edits.

### 2026-08-07T09:33 — SC-4 v4 FROZEN version-rule reaffirmed; Hicks re-HOLD + NEW-2 DONE

- **Ripley SC-4 v4 FROZEN (parent final/monotonic, `ripley-SC4-v4-FROZEN.md`):** fully consistent with the recorded canonical — handled by dedup on the single 09-06 block. Made the **VERSION RULE** explicit (v4 supersedes v1 `targetHandles`, v2/v3 slot variants, and every queued handles/slots message regardless of delivery order; future contradicting messages are stale by definition — record not re-opened); added `EncodeClaimWindowClosed` alongside `EncodePickupCleared` in the server clears; re-confirmed heading 09:33 + cited `ripley-SC4-v4-FROZEN.md`.
- **Hicks 09:30 memo:** (1) HOLD `targetSlots` again — disregarded a 09:25 "Lead → `targetHandles`" message under the parent's own frozen lock (pivot ~15; no v5/un-freeze); matcher unchanged, fails-closed anyway (numeric `thing.key`). Recorded on the closure block's "first test" note. (2) **NEW-2 DONE (frontend):** `world.ts::driveManualDealChain` auto-take loop + dead helpers REMOVED; only take path = real endpoint click gated by `wallTileInteractive` (exact `targetSlots[0]` ∧ F2 top ∧ phase/manual/seat); interim wall inert/fail-closed = intended v4; AUTO unaffected. Recorded as UPDATE 09:30 on the 09-23 NEW-2 bullet. Remaining: Hudson G17 re-point + Bishop `targetSlots` dependency.
- **Inbox artifacts:** 3 non-memo `bishop-r1-e1-e4` patch/test files (already dispositioned in the 09-18 block) moved to gitignored `inbox/quarantine-r1e/` — preserved, out of the active inbox, not applied (I touch no production code).
- Deferred the Hicks-history cross-agent note this turn to avoid re-breaching the freshly-folded 15,360 B gate (Hicks at 15,344 B); the durable record is in decisions.md, which Hicks cites as source of truth. Raw memo archived verbatim.

- **Bishop→Vasquez F1 escalation (09:33, from quarantined r1e worktree):** recorded the **F1 wall-frame drift diagnostic** (dealer-relative `[14,13,14,13]` BreakPoint vs seat-absolute `[14,14,13,13]` slotmap ⇒ break/`targetSlots` highlight one column off for 7/16 dealer×break-wall combos) + the **OPEN Vasquez canonical-frame ruling** (a vs b, 3 fix options; impl owner = bishop-2 slotmap lane). Tagged provenance: the memo's E1 `targetTileIds` / E2 batch `targetSlots` / F1 golden stay REJECTED per 09-18 (do NOT merge as authority); only the frame diagnostic + E3/E4 salvage. Confirms `EncodeClaimWindowClosed` (BE-4) matches the clear just wired into the v4 contract. New 09-33 F1 block; raw memo archived.

### 2026-08-07T09:40 — Ripley formally RETRACTS the 08:39 targetHandles request; v4 targetSlots reaffirmed (churn source closed)

Ripley (integration coordinator) formally RETRACTED his own 08:39 `targetHandles` pickup request (it cited the pre-v4 `ripley-SC4-pickup-schema-IMMUTABLE.md`; superseded/stale under the VERSION RULE) and CONFIRMED (B) single-trigger-slot `pickup.targetSlots` (SC-4 v4) as the sole canonical pickup key — explicitly affirming the Scribe integrity guard that held the record to `targetSlots` throughout the ~13-pivot churn. Fully consistent with the recorded canonical ⇒ dedup on the single 09-06 block:
- Heading re-confirmed **09:40**; the "Validates the whole reconciliation" clause now records the **formal 09:40 retraction** closing the churn SOURCE + confirming the integrity guard.
- SUPERSEDED/VOID list extended with **Ripley's 08:39 `targetHandles` request (FORMALLY RETRACTED 09:40)**.
- Permanent split reaffirmed: `targetHandles` = SC-2/G19 opaque per-viewer hidden-`things` keys ONLY, never the pickup key.
- Cross-agent: appended a retraction note to Ripley's history; folded his 06-04 Docker-deploy + STATE-OF-GAME sections → archive (19,553 → 12,187 B, back under the 15,360 gate, non-lossy). No inbox memo (direct message). No commit / no production edits.

### 2026-08-07T09:38–09:44 — Bishop backend integration status; Hicks FE-6 canvas fix; pivot ~16 held

- **Bishop backend integration (09:38, @200cad4):** status correction — NOT starved (4 prod files +268/−16, 20 UAT tests GREEN, suite 4701/0/2). SC-1 ✅ (`match[0]` gameType CHANGSHA + dealMode/dealType + variant superset); BE-2..5 + R-1 landed (BE-4 `EncodeClaimWindowClosed` wired; BE-5 own-hand face-up). R-1 pickup NOT yet v4-conformant (still `targetTileIds`+highlight → owes len-1 `targetSlots`, drop targetTileIds; Frost G17 HOLD stays). **SC-2 📐 design posted** = NUMERIC `HIDDEN_HANDLE_BASE`+Fisher–Yates perm — 🚩 flagged DIVERGENCE vs bishop-2's landed HMAC-**string** `OpaqueTileHandleProvider` (09-26); Ripley to pick the string provider (G19-permitted) as canonical. SC-3 ⚠️ blocked on Hicks `worldPos` oracle + §F1 Vasquez ruling. New 09-38 block.
- **Hicks FE-6/G20 (09:38):** canvas letterbox/dead-space fix — `main-view.ts::updateViewport` fills full container + new `updateCameraProjection` adapts aspect (perspective + ortho-contain), hit-testing preserved, removes desktop pillarbox. tsc clean, 102 tests. New 09-38 block.
- **Hicks HOLD pivot ~16 (09:43–44):** a 09:43 "handle-only `targetHandles`, delete targetSlots branch" message — itself contradicting a 09:41 "FINAL" note (targetSlots-correct) 2 min earlier — DISREGARDED per v4-FROZEN (stale cross-message drift). Extended the closure block's "first test" note (dedup). Hicks-history cross-agent note deferred again (at 15,344 B, 16 under gate; decisions.md authoritative).
- All 3 raw memos archived verbatim. No commit / no production edits.

### 2026-08-07T09:45–10:03 — F1 rules SIGN-OFF + SC-2 canonical resolved + integration status (15-memo batch)

Processed a 15-memo stream (Ralph ×5, Frost ×4, Hicks ×3, Ripley ×1, + 2 re-drops) plus the F1 sign-off directive.
- **F1 RULES SIGN-OFF (Vasquez):** canonical wall frame = seat-**ABSOLUTE `[14,14,13,13]`**, NO dealer rotation, col ascending/top-first; 44-case oracle `B=(dealer+(sum-1)%4)%4, col=capacity[B]-sum, layer top`; old dealer-relative `[14,13,14,13]` diverges 16/44 ⇒ REJECTED; Bishop's Option-A OVERRULED. RESOLVES the 09-33 escalation. New 10-02 F1 block + addendum (split delivered it; Frost code-gate ACCEPTED 44/44 but golden `ExpectedAnchorOracle` VACUOUS → populate with Vasquez's table; stale `[14,13,14,13]` refs in `BreakPointService`/`ChangshaToAutotableTranslator`/`changsha-wall-worldcoord-polyline.spec` to fix; Group A 10 test-encoding fixes `col*2+(1-layer)` + Group B 1 latent bot livelock seed 111866 → don't revert F1). Corrected all stale "52-case" → **44** (F2 golden refs).
- **SC-2 CANONICAL (resolves my 09-38 divergence flag — flag was correct):** numeric-permutation REJECTED (precision/statefulness/entropy); canonical = bishop-2 HMAC-SHA256 base64url STRING `OpaqueTileHandleProvider`; **Frost B1 RESOLVED** (HKDF-derived MAC key, no raw-JWT, `DoesNotUseRawSecretAsMacKey` test) ⇒ APPROVE 36/36. Integration still owes BuildThingEntry wiring + dedicated secret + Hudson raw-WS P1-P5. Hicks SC-2 frontend audit: keyspace SAFE (`partitionThingEntries`, 109 tests) but ⚠ render-pipeline constraint (needs anonymous back-Thing pool, recommend (a)) + co-sequencing risk (co-land or gate string-key emission). New 10-02 SC-2 block + frontend-audit block. F2 flip RED-s 9 primary StuckTurn C-3 tests → co-land the inverse-helper fix (not skip).
- **Integration status (10-00):** SC-4 rename done, cardinality still batch (owes len-1 + batchPreviewSlots); `-slotmap` OVERLAP → retire (F2 from bishop-2 helper, refold F1 into bishop-1); int-f2/int-provider/int-keysep/int-inverse9/prod-f1f2g19 open. New 10-00 block.
- **NEW-2 fully DONE (Hicks P0 09:52):** whole `driveManualDealChain` deleted (incl auto-roll); sole `emitTakePickup` caller = pointer intercept; `pickupTakeCommand{seatIndex,count}`; 104 tests. Extended the 09-23 NEW-2 bullet.

All 15 raw memos archived verbatim. No commit / no production edits. decisions.md ~114 KB (all current-wave; content-consolidation of resolved older sections warranted once the wave settles).

### 2026-08-07T10:08–10:10 — F1 golden gate LOCKED; SC-2 co-land formalized (5 render + 6 wire gates)
- **Frost F1 code gate LOCKED (10:08):** `SlotMapWallGoldenTests.ExpectedAnchorOracle` must be the hardcoded §2.4 44-value `(dealer,sum)→(seat, StacksPerSeat[B]−sum, top)` table; REJECT if self-derived/vacuous, any dealer-relative rotation persists, or any case ≠ `(B, StacksPerSeat[B]−sum, top)`. Enhanced the F1 addendum golden bullet.
- **SC-2 hard CO-LAND gate (Ralph 10:08+10:10):** STEP-2 opaque-string emission (`s2-buildthingentry`) BLOCKED until Hicks's anonymous-back pool + **5 render gates** (mixed snapshot / 108 count / atomic reveal / hidden-suppress / target raycast) AND **6 emission wire criteria** (entitlement set / slot-canonical order / hidden whitelist / public numeric stability / 108-unique / 🚩 no draw-order tuple — currently VIOLATED by tileId-keyed things) are green, co-landed in one frame. Enhanced the SC-2 co-sequencing bullet. G19 stays BLOCKING for UAT closure.
- 3 more raw memos archived verbatim. No commit / no production edits.

### 2026-08-07T10:14 — Helper outcome confirmed (Frost APPROVE, HKDF); merge-only + G19/SC-3-not-closed guard
Parent confirmation of the bishop-2 helper outcome (fully consistent with the 10-02 SC-2 record ⇒ dedup): Frost APPROVE after HKDF key separation; provider+F2 primitive 36/36, Release clean; **merge ONLY with the 9 StuckTurn inverse-test updates `col*2+(1-layer)`**; **do NOT mark G19/SC-3 CLOSED** until the canonical projection (`BuildThingEntry` string wiring + STEP-2 render co-land) + oracles (F1 44-table + F2 reachability + SC-3 production golden) + Hudson raw-WS P1–P5 all land. Added "Release clean" + a crisp "helper outcome CONFIRMED" guard line to the 10-02 SC-2 block. No commit / no production edits.

### 2026-08-07T10:21 — Ripley retracts 08:49 + PERMANENT STANDING RULE; SC-2 backend impl (flag-gated) + pickup-key v4 violation flagged; r1e full-quarantine

- **Ripley 10:21 (primary):** formally RETRACTED the 08:49 `targetHandles` instruction (and reaffirmed the 08:39 retraction); asked to resolve the "08-49 contested" framing → SUPERSEDED (targetHandles-for-pickup VOID). No live 08-49 block exists (churn already collapsed into §09-06 at 09:06), so applied via dedup on the v4 block: added 08:49 to the retracted list, re-confirmed 10:21, and recorded a **🧭 PERMANENT STANDING RULE** — any message from anyone/any-timestamp/any-label naming `targetHandles` (or a batch/count-length array incl. `targetTileIds`-as-handles) as the PICKUP key is stale by definition; never record canonical, cite the rule, do NOT escalate. `targetHandles` = SC-2 `things` keys only.
- **SC-2/G19 backend IMPLEMENTED (Bishop, flag-gated `Privacy:OpaqueHandles` default FALSE):** `ChangshaPrivacyProjector` HMAC-string hidden-key projection, D-2 entitlement in `BuildThingEntry`, P1-P5 + non-brute-force GREEN. New 10-1x block. **🚩 Applied the STANDING RULE:** the memo's `pickup.targetTileIds`=per-viewer-handles match key is a v4 VIOLATION — flagged, NOT recorded canonical; pickup stays `targetSlots` len-1; Bishop owes the drop. **🚩 Two HMAC-string providers** (canonical `ChangshaPrivacyProjector` raw-secret vs bishop-2 `OpaqueTileHandleProvider` HKDF-approved) to reconcile (B1 key separation). Env incident (disk-full, recovered, 926 GREEN, prune-artifacts warning) recorded.
- **Frost RV-2 SC-2 frontend:** security/no-inference axis APPROVED (partition/face-suppress/opacity/reconcile-safety, 45 tests); render pipeline HOLD (onHiddenThings stub; back-pool/suppression/reveal/108 unmet); co-land required (flag OFF ⇒ SC-2 0-HIT end-to-end).
- **r1e (Ripley 10:1x):** FULL QUARANTINE / ZERO SALVAGE — canonical already has E3+E4; E1/E2/F1 outdated. Updated the 09-18 block (was "E3/E4 reusable"); the 3 `inbox/quarantine-r1e/` patch files fall under do-not-integrate.

All 3 raw memos archived verbatim. No commit / no production edits.

### 2026-08-07T10:20–10:2x — SC-2 render pool IMPLEMENTED (Hicks); Frost merged-backend 4-criteria sign gate
- **SC-2 render pool (Hicks 10:20, option a):** anonymous hidden-back Thing pool IMPLEMENTED (108 backs @ contiguous 108–215, InstancedMesh→216; hidden-real suppression; atomic no-tombstone reveal; exact-108 invariant; backs raycastable for `targetSlots[0]`). 115 contract tests; addresses Frost's render HOLD at contract level. Pending Hudson browser G17/G19 + co-land with Bishop's flag-ON HMAC wire. New 10-20 block.
- **Frost merged-backend SIGN GATE (10:2x):** 4 pass/fail criteria for the ONE shipping `uat-backend` (split ∪ Bishop): (1) single frame `[14,14,13,13]`; (2) F1 anchor golden — populate `ExpectedAnchorOracle` with Vasquez's 44-row table + DELETE Bishop's tautological `BishopUatBackendContractsTests:144-148` ("delete tautology without populating = still zero enforcement"); (3) F2 top-first + Group A asserts (no greenwash); (4) `targetSlots` len-1 (no `nextTileSlots`/`targetTileIds`-handles). SC-2/G19 separate-blocking. Mutation re-review (fail-if-rotated). New 10-2x block.

Both raw memos archived verbatim. No commit / no production edits.

### 2026-08-07T10:30 — Ripley confirms record finalized; SC-2-helper sub-lane spawning (G19-blocking)
Parent CONFIRMATION (no record change to pickup — already correct): pickup = `pickup.targetSlots` (public slot, len-1, top-first Wall[0]); `targetHandles` retired from pickup; two-field distinction documented; 08:49 block VOID/RETIRED; 10:21 STANDING RULE in effect; no pickup-key reopen. Orchestration: F2-slot-map sub-lane DONE (split complete); Ripley now spawning the **SC-2-helper sub-lane (`OpaqueHandleService`)** — the BLOCKING G19 item; pickup rename = Bishop nudge (in progress); parent will log the SC-2 split completion when it lands. Recorded as a UPDATE 10:30 bullet on the 10-00 integration-status block. No commit / no production edits.

### 2026-08-07T10:30 — USER UX work item: persistent one-click New Game (supersedes FE-1 hide-#new-game)
User directive: a persistent, simple, ONE-click "New Game" VISIBLE in Changsha — preserves config/player/same seat, mints a fresh gameId, reconnects/starts authoritative Auto|Manual with NO setup/lobby/Deal/Take-Seat clicks in the seated flow, works mobile/desktop/disconnected/GameComplete, and releases the old game. SUPERSEDES the FE-1 plan that hides `#new-game` as a relay-only control (other relay controls stay hidden; only `#new-game` promoted); extends the 08-06 stale-game `#new-game` escape to always-available. Owners: Hicks (frontend control/flow) + Bishop (authoritative fresh-gameId start + same-seat preserve + old-game release). New 10-30 block + FE-1 hide-list corrected. No commit / no production edits.

### 2026-08-07T10:31 — New Game work item operationalized + Frost grounded r1e/pickup findings
- **Frost RV-2 (grounded):** r1e HARD REJECT confirmed by file/line; E3/E4 verified already in canonical (resolves Frost's F4 dead-EncodeClaimWindowClosed). NEW: canonical pickup still gate-#4 non-compliant — `targetSlots` is a BATCH (not len-1); `targetTileIds` still emitted (opaque w/ privacy ON but RAW in sandbox = leak); stale `:263` comment; test `:138`. Grounds the 10-1x v4-violation flag. New 10-3x block.
- **Ralph New Game blocker (operationalizes 10-30 user directive):** persistence GAP root = `#new-game` (index.html:327) in the FE-1-hidden relay sidebar; need one persistent button in ALL phases via the authoritative `location.replace`+fresh-gameId path (not legacy relay RC-9). Fresh-id+config ✅; seat-preserve/no-extra-clicks/old-release/debounce unverified. Owners Ferro/Hicks/Bishop/Hudson + 5 per-phase Hudson acceptance gates + 7 tracking rows. New 10-31 block.
Both raw memos archived verbatim. No commit / no production edits.

### 2026-08-07T10:2x — SC-2 split COMPLETE (OpaqueHandleService); 🚩 three-provider convergence flagged
Ripley's 2nd armed split landed: `OpaqueHandleService.cs` (`-sc2` worktree, agent sc2-opaque-handles), 22/22, Release 0/0 — `"h_"+base64url(HMAC-SHA256(dedicated OpaqueHandle:Secret, gameId|playerId|tileId))[..22]`; ProjectThingKey(viewer,game,tile,entitled); wiring small (PlayerId available at the 3 FilterEntriesForViewer call sites 1416/1637/1666). Makes all `things` keys strings; Hicks ready. **🚩 Scribe integrity flag:** THREE OpaqueHandle impls now exist — OpaqueHandleService (-sc2, Ripley-directed), OpaqueTileHandleProvider (bishop-2, HKDF, Frost-APPROVED), ChangshaPrivacyProjector (bishop-1 canonical, raw-secret/B1-footgun) — integration MUST converge to ONE + satisfy Frost's B1 key-separation (the -sc2 provider uses a dedicated secret directly, not HKDF; Frost to rule). Both split targets DONE (F1/F2 + SC-2); Bishop residual = merge both file-sets + wire SC-2 + targetSlots len-1 + kill proxy golden + Group A updates. New 10-2x block. No commit / no production edits.

### 2026-08-07T10:34 — SC-2 reconciliation: projector→HKDF-helper delegate; privacy default-ON; P1-P5 live-raw-WS-only
Ralph reconciled the 10-2x three-provider flag: `ChangshaPrivacyProjector` raw crypto DELETED → delegate to the HKDF helper (closes int-keysep); ONE HKDF-keyed crypto (residual OpaqueTileHandleProvider-vs-OpaqueHandleService file choice for Ripley/Bishop). NEW: privacy DEFAULT-ON for Changsha (currently default-off=numeric leak, AutotableWsEndpoint:132). NEW validity flag: P1-P5 must be LIVE raw-WS (`changsha-privacy-opaque-handles.spec.ts`, attachRawWsCapture, ctxA/ctxB) with privacy default-ON — provider unit-only P1-P5 green is INVALID (render-proxy-class). Hicks pool DONE. 5-step co-land sequence. New 10-34 block. No commit / no production edits.

### 2026-08-07T10:35–11:0x — Convergence batch (17 memos): F1 two-gate, artifact reconciliation, pickup co-deriv, New Game + deal P0s, assembly manifest
Processed 17 memos + the parent 10:58 pickup-final confirmation.
- **Parent 10:58:** targetSlots canonical re-confirmed; recorded the REOPEN GATE (supersede 08:59 DEFINITIVE by name AND Frost RV-2 re-sign — neither exists), the 09:02 targetHandles/Vasquez-canonical message VOID/crossed, and Vasquez's 10:47 explicit targetSlots word (full consensus). Enhanced the v4 block (no standalone 09-02 block exists — churn collapsed at 09:06).
- **F1 = TWO-GATE:** Gate-1 engine-frame SIGNED (Vasquez 44-row oracle + Frost 44/44 three-ways + 16/44 bite; F2 reachability Frost-PROVEN #3); Gate-2 visual-perimeter OPEN (R1-vs-R2 break off-by-one + CCW handedness — browser oracle vs Vasquez worked examples). New 10-4x block.
- **BACKEND ARTIFACT RECONCILIATION:** CANONICAL SET = ChangshaPrivacyProjector (seam, wired) + OpaqueTileHandleProvider (HKDF crypto, Frost-approved) + slotmap-split wall; QUARANTINE sc2 OpaqueHandleService + bishop-2 F2-only wall; Ripley RETRACTED "wire sc2". Frost privacy HOLD (B-CRYPTO delegate-HKDF + B-DEFAULT default-on + K-STABLE) → RESOLVED-at-design by the key-source resolution (dedicated OR JWT-as-HKDF-IKM). Entitlement SET Frost-aligned (sharp 暗杠/加杠). New 10-5x block + assembly manifest 11-0x.
- **Pickup co-deriv SHIPPED** (ComputeWallFrontSlots single-pass) but targetSlots still BATCH → len-1 cap owed (Hudson E1 asserts length===1). SC-2 key format value-based confirmed (parseRealTileId). New 10-4x block.
- **New Game P0 IMPLEMENTED** (Hicks + BE-3 confirmed) addressing the 10-30 directive; **🔴 NEW-1 bot-dealer hand-1 manual stall = likely ORIGINAL live symptom** (StartGameAsync manual branch doesn't schedule bot-dealer roll); UAT synthesis = NEW-1 + RC-9 + NEW-2. New 10-4x block.
- **Assembly manifest** (11-0x, for Apone): backend 3-worktrees→1 file-copy + Bishop pending list; frontend∪ui→build; tests union.
All 17 raw memos archived verbatim. No commit / no production edits.

### 2026-08-07T11:02–11:08 — G4/G6 rulings; Frost gate-#4 targetSlots len-1 VERIFIED DONE
- Ripley G4 ruling (cross-corner continuity REQUIRED, frontend geometry fix, Hudson metric correct = the Gate-2 visual-perimeter item) + G6 ruling (own-hand face-up via SC-2 ENTITLEMENT not client seat-guess; ties G6↔G19) + E1→targetSlots retarget + test-lane canonical (/data/source uat-tests, 14 files). New 11-02 block.
- **Frost gate #4 = DONE ✓ (verified 11:08):** Bishop capped targetSlots to len-1 (`new List{wallFrontSlots[0]}`) + batchPreviewSlots display-only + NO targetTileIds/targetHandles + E3 tombstones; resolves the 10-4x/10-3x batch flag. Reachability rides in on the slotmap merge (pre-merge still o%2). Updated the 10-4x pickup block.
No commit / no production edits.

### 2026-08-07T11:12–11:45 — Canonical ASSEMBLY START + 25-memo peak convergence batch
Recorded the canonical assembly START (`/tmp/…-uat-integration`, ripley-1 sole editor, @200cad4; approved sources + exclusions) and drained a 25-memo convergence surge:
- **R-1 WALL rules-half DONE:** Vasquez F2 golden SIGNED + F1 anchor CONFIRMED (temp-paste 44 triples → 223/223 non-vacuous, TRIPLY confirmed); paste=bishop-1's; persisted golden still VACUOUS-on-anchor until paste (Frost caveat). F2 reachability bound to Vasquez 17-pickup oracle (748 assertions). Wall-merge EMPIRICALLY PROVEN (build 0/0 + 223/0 + StuckTurn 25/25 after ONE-line Group A helper flip; four-half-walls fixed). Gate-2 R1/R2 FINAL (Gate-1 frame+non-rotation SIGNED unconditional; abs-col R1-PROVISIONAL; 3 user-obs outcomes). Scaffold APPROVED.
- **G19 = ACTIVATION** (provider approved); fail-closed both axes; 🔴 SPECTATOR-FRESH deanon hole (cookie-less → fresh ephemeral id).
- **Frost gate-#4 STRUCTURE done (Ripley 11:23 lock)** — but DO NOT sign uat-backend PRE-MERGE (still base o%2/[14,13,14,13]); merge MUST take slotmap versions of the 2 wall files (resolved my assembly-start flag: slotmap WALL is APPROVED, Ralph retracted "orphan").
- **G6/R-B CORRECTED → BACKEND (BE-5, already done)** — supersedes the 11-02 frontend ruling; R-B removed from Hicks.
- **NEW-1/NEW-2 LOCKED contract + ★ANTI-HAPPY-PATH rule** (must test non-dealer+bot-dealer reaching 13 AND real-click pickup). **🔴 NEW-2 = STALE DIST** (source already fixed; served bundle predates it — integrator rebuild needed; same theme as four-half-walls).
- **New Game FE COMPLETE** (145 tests, data-action delegation, seat-0-safe handoff); 2 Bishop backend contract decisions needed (?seat= auto-take blast-radius 82 tests; old-game-release abandonment signal).
- Bishop F2 two-lane split (map=slotmap, emission/take=bishop; apply BOTH patches). Ralph assembly monitor: t=0 clean, 7 flag categories.
Recorded 6 new blocks + G6 correction + assembly-start; all 25 memos archived verbatim. No commit / no production edits.

### 2026-08-07T11:54 — SC-3 Gate-2 authoritative memo cited + pivot ~17 targetHandles reversal held
- **SC-3 Gate-2 authoritative reference** = `Frost-RV2-FINAL-R1R2-handedness-position` (cited on the 11-2x wall block): handedness HARD RULE = seat B col-max toward seat B+1 centroid (rules-certified §2.1, hard-assertable); R1/R2 separate — R1 provisional ship / MIRROR frontend fix / R2 backend + oracle reissue; supersedes the earlier unconditional memo.
- **Pivot ~17 (11:52):** Ripley (sibling) re-cited the superseded `ripley-SC4-pickup-schema-IMMUTABLE.md` reversing to `targetHandles`; Bishop DECLINED (sibling≠parent) and held `targetSlots`. Recorded per the STANDING RULE + REOPEN GATE. NEW top authority: the USER's own 11:29/11:43 verbatim mandate (`targetSlots` len-1, NO handles). Enhanced the v4 "first test" note. No commit / no production edits.

### 2026-08-07T11:53–11:54 — R-B done both sides; R-A(G4) layout-split; assembly scan flags slotmap-WALL not pulled
- R-B (G6 face-up) DONE both sides: backend BE-5 (authoritative) + Hicks frontend entitlement-based `handFaceRotationIndex` (seat-guess deleted, 156 tests). R-A (G4 corners) = LAYOUT change (corner solution O14/O13 = 0.97p but collides with discards y≈46-51) → SPLIT to a geometry agent (setup-slots + wall-corner-ring RED gate + Gate-2 oracle).
- **Ralph assembly SCAN (validates my 11-45 flag + gate-4 pre-merge warning):** ✅ privacy default-ON + crypto-swap-to-HKDF + SC-4 len-1 + F2 1-(o%2) all correct in assembly; 🚩 3 WALL-source flags — F1 BreakPointService NOT pulled (still base [14,13,14,13]); wrong wall-test source (`-helpers` TopFirstTests present, `SlotMapWallGoldenTests` missing); invalid proxies present (wall-contiguity-152/c5-wall-forward). Assembly in-progress (23 dirty); Ralph re-scans on settle. Frontend SC-2/G19 verified ready (only backend activation remains). New 11-53 block.
No commit / no production edits.

### 2026-08-07T11:56–11:58 — Parent file-disjoint backend split + Ralph flag correction
Parent formalized file-disjoint backend ownership (bishop-2 = 4 files: F2 AutotableSlotMap + OpaqueTileHandleProvider + tests; bishop-1 = F1 BreakPointService[from slotmap] + translator/protocol/WS/runtime/privacy + integration tests[from slotmap]). Sourcing refinement: F2 AutotableSlotMap SOURCE from bishop-2 (slotmap's is redundant/identical); slotmap = hand-over source for F1+goldens+StuckTurn+bot tests only. Ralph FLAG-2 corrected (KEEP AutotableSlotMapTopFirstTests — complementary F2-primitive test; the "not its F2 AutotableSlotMap" meant the source file). Standing flags: asm-stale-f1, asm-selective-f1golden, asm-oracle-paste, asm-proxies, group-a. Empirically pre-validated (223/0 + StuckTurn 25/25). New 11-56 block. No commit / no production edits.

### 2026-08-07T12:11 — RECORD DISCIPLINE engaged (parent): HOLD canonical records during active integration
Parent directive: keep canonical records unchanged during integration; record only the persisted integrated artifact + source/live gate results when Ripley reports SETTLED; do not preserve transient assembly observations as completion evidence. Applied:
- Recorded the 12-11 record-discipline block: tagged 11-45/11-53/11-56 as TRANSIENT/in-flight (sourcing churning — 11-56 "take F1/goldens from slotmap" already superseded by 12:05 "slotmap = ORPHAN, author F1 canonically"); listed the HELD memos (four-half-walls fix, helper-ready/integration-order, slotmap-orphan-rehome, tombstone UI-teardown, variant/dealMode wire-authority, Vasquez F2 primitive signed) as archived-not-recorded pending settle; reaffirmed the frozen canonical contracts.
- **Pivot ~18 (Bishop 11:55 targetHandles code-switch, "Lead FINAL via Vasquez"):** HELD/contested — reopen gate not met (no 08:59-by-name supersession + no Frost re-sign), contradicts user 11:29/11:43 + Vasquez 10:47 + Hicks matcher, and the parent's own 12:11 said keep canonical unchanged. Canonical pickup stays `targetSlots` pending DIRECT parent confirmation to Scribe.
- Archived all 8 incoming memos verbatim (non-lossy); no new transient assembly/convergence blocks. Holding for the settled-artifact recording. No commit / no production edits.

### 2026-08-07T12:14 — Pivot ~18 RESOLVED: Ripley contract-catch re-confirms targetSlots (reopen gate NOT met)
Ripley (direct, 12:14) caught Hudson's G17-D2 targetTileIds/handles regression, re-confirmed SC-4 v4 `pickup.targetSlots` (public slot, len-1), and stated the reopen gate "explicit parent supersession + Frost RV-2 re-sign — NEITHER EXISTS" — directly refuting Bishop's "Lead FINAL 11:55 via Vasquez" (a mis-attribution; Vasquez's actual 11:52 word = the SLOT field; Frost 11:48 interlock = targetSlots). Both Bishop's 11:55 switch (pivot ~18) and Hudson's G17-D2 are SUPERSEDED by SC-4 v4 → revert to targetSlots. Updated the v4 pivot note (18th vindication of the hold). No commit / no production edits.

### 2026-08-07T15:56 — Integration assembly VERIFIED (persisted artifact) + termination matrix; drained the 96-item backlog
After the 12:11 HOLD, the assembly reached the candidate stage. Recorded the persisted-artifact + gate results per the directive (superseding the transient assembly scans):
- **Integration VERIFIED (Ripley 13:44/15:10/15:52):** `/tmp/…-uat-integration` folded ALL backend lanes contract-correct line-by-line (F1 [14,14,13,13], F2 1-(o%2), SC-4 targetSlots len-1 co-derived, 44-oracle non-vacuous + self-ref proxy deleted, G19 HKDF-delegate + default-ON + spectator-fresh + IKM-or-throw); merge-correctness invariant holds tree-wide ⇒ 11-53 FLAGs 1/2/3 RESOLVED (marked transient). :18084 candidate serves the FIXED build (rebuilt bundles; buildSha=200cad4 BENIGN under no-commit — certify by behavior/bundle-hash not buildSha). New 15-1x block.
- **Termination matrix (Ripley 15:58):** 12 axes — ✅ signed (#1 wall backend Gate-1, #7 pickup SC-4, #11 new-game A/B); 🔶 live-verify pending (#3-6 gameplay, #8 privacy, #9 G5, #12 build/serve); ⏳/🧍 open (#2 wall VISUAL Gate-2 + USER eye, #10 G20 canvas). New 15-58 block.
- **Bishop reconciliation:** the "4701" was his own 09-38 snapshot (not a blocker); worktree now 5096/0/2, both items DONE (targetSlots len-1 + HMAC-string; numeric absent). Authoritative patch moved to inbox/backend-patches/ (preserved; Scribe does not apply code).
- **Pivot ~18 FULLY RESOLVED:** Bishop REVERTED to targetSlots 12:16 (parent-locked); 19th+ vindication of the hold.
- Drained the backlog: 93 .md memos archived verbatim; 3 code patches moved to inbox/backend-patches/. No new transient blocks beyond the verified-artifact + matrix. No commit / no production edits.
